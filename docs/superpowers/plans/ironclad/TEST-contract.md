# TEST: pin durable branch-registration park telemetry

target_repo: jleechanorg/dark-factory
Execution role: administrative subtask of c4zhq; no factory label.

Environment: Linux factory-assigned c4zhq worktree; require P0 before coding. Run RECORD-BASE before editing, using the proof invocation from execution-reference.md. Worker must use its validated account scope. Never create a Mac coding checkout.
Goal: create the three named integration regressions, commit expected RED without production edits. Modify only daemon/tests/tick_integration.rs and the exact required fixture changes in daemon/tests/common/mod.rs. Dependency: P0 admission; resolve P0's ID from /home/jleechan/roadmap/af-first-ready-20260913/beads.json. Target 100 changed test lines; the documented integration-fixture overrun is allowed only at the counted budget in c4-exact-edits.md.
Reuse: run_tick_emits_parked_human_held_for_unmapped_repo_dispatch_failure in daemon/tests/tick_integration.rs; StateStore and SqliteStateStore; exact anchors, fixture data and snippets in /home/jleechan/roadmap/af-first-ready-20260913/c4-exact-edits.md.
Steps: 1. Run P0. 2. Read c4-exact-edits.md in full and apply only its TEST snippets at its named anchors; no invented fixture values or production seam. 3. Run C4-RED; require the permanent collision assertion C4_EXPECT_DURABLE_PARK_TELEMETRY and both safety tests passing. 4. Run COMMIT-TEST, then RECORD-TEST, then rerun C4-RED. Independent verifier reproduces this RED at test_sha before this subtask closes. This administrative TEST subtask is never separately factory-labeled or promoted to READY.
RED: C4-RED exits 0 with PASS C4-RED only for the expected assertion failure; a compile error is not RED.
GREEN: n/a for the changed behavior — IMPL owns GREEN. TEST completion is independent reproduction of committed expected RED plus exclusive test ownership.
Do not: do not change daemon/src or weaken assertions; instead report a contract mismatch. Do not expect a failing test-only commit to pass the parent READY gate; instead continue to the dependent IMPL inside the same parent worker after independent RED verification.
Prior-failure warning: a permanent persisted park was mislabeled transient; service presence and self-reported green did not establish actual dispatch or account scope.
Stop when: a command returns an unexpected result; retry the identical command once, then report to the parent. Missing artifact, instruction conflict, unsafe/unscoped launch, or unsupported bootstrap means stop the affected lane without edits. Parent diagnoses within the eight-hour execution time-box. No repeated whole-plan review. Default criterion verdict FAIL. A worker reporting failure is a valid lane outcome, not goal completion.
Report: write evidence/TEST-report.md with the lane name, exact command, committed SHA, raw receipt path and verbatim PASS/FAIL markers, then run `br --db /home/jleechan/.local/state/dark-factory/.beads/beads.db --no-auto-flush --no-auto-import comments add dark-factory-c4zhq "$(cat /home/jleechan/roadmap/af-first-ready-20260913/evidence/TEST-report.md)"`. Never include credentials. Only the independent verifier closes the subtask after reproducing its criteria.

| Criterion | Check command | External anchor | Independent verifier |
|---|---|---|---|
| Committed expected RED is independently reproduced | `python3 -c 'from pathlib import Path; t=Path("/home/jleechan/roadmap/af-first-ready-20260913/execution-reference.md").read_text(); exec(t.split("## C4-RED\n",1)[1].split("```python\n",1)[1].split("```",1)[0])'` | Actual committed test execution or fresh Linux/GitHub state as defined in the reference; index JSON alone is insufficient. | Fresh independent factory Codex reviewer; default FAIL until reproduced. |
| Held identities are unchanged | `python3 -c 'from pathlib import Path; t=Path("/home/jleechan/roadmap/af-first-ready-20260913/execution-reference.md").read_text(); exec(t.split("## HOLDS\n",1)[1].split("```python\n",1)[1].split("```",1)[0])'` | Actual committed test execution or fresh Linux/GitHub state as defined in the reference; index JSON alone is insufficient. | Fresh independent factory Codex reviewer; default FAIL until reproduced. |
| Committed RED contains only the owned test files | Run TEST-OWNERSHIP using the exact proof invocation in execution-reference.md. | Git object ancestry and full committed diff. | Fresh independent factory reviewer; default FAIL. |
