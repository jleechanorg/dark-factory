# IMPL: report the durable register-branch park truthfully

target_repo: jleechanorg/dark-factory
Execution role: administrative subtask of c4zhq; no factory label.

Environment: the same Linux c4zhq worker worktree at test_sha read from /home/jleechan/roadmap/af-first-ready-20260913/evidence/revisions.json; prove `git rev-parse HEAD` equals it before editing. Account admission must still match the running release. No separate branch or factory intake.
Goal: fix truthful permanent-park telemetry. Modify only daemon/src/tick.rs. Dependency: the TEST administrative bead, whose ID is read from /home/jleechan/roadmap/af-first-ready-20260913/beads.json and whose committed RED is independently verified. Target at most 50 changed non-test lines; no test changes.
Reuse: dispatch_ready_with_vcs already persists BranchRegistrationConflict in daemon/src/dispatch.rs; reuse existing emit and HumanHeld vocabulary in tick.rs. Exact insertion anchor and content: /home/jleechan/roadmap/af-first-ready-20260913/c4-exact-edits.md, production section.
Steps: 1. Run TEST-OWNERSHIP, then C4-RED before edits. 2. Apply only the production snippet at its exact anchor from c4-exact-edits.md. 3. Run C4-GREEN, run COMMIT-IMPL, then RECORD-IMPL. 4. Independent verifier re-executes C4-GREEN and OWNERSHIP at impl_sha. After these pass, the same parent worker runs PUBLISH from execution-reference.md and continues normal factory review gates.
RED: the preceding test_sha reproduces C4_EXPECT_DURABLE_PARK_TELEMETRY before the source edit.
GREEN: PASS C4-GREEN plus PASS OWNERSHIP at the same committed impl_sha.
Do not: do not change dispatch.rs, recovery policy, reasons, schema, branch ownership, or tests; instead retain their current contract. Do not emit a park for transient registration or failed park save; instead retain the existing transient/error path. Do not hand-push a different branch; instead use the c4zhq factory-owned branch and normal fast-forward publishing.
Prior-failure warning: a permanent persisted park was mislabeled transient; service presence and self-reported green did not establish actual dispatch or account scope.
Stop when: a command returns an unexpected result; retry the identical command once, then report to the parent. Missing artifact, instruction conflict, unsafe/unscoped launch, or unsupported bootstrap means stop the affected lane without edits. Parent diagnoses within the eight-hour execution time-box. No repeated whole-plan review. Default criterion verdict FAIL. A worker reporting failure is a valid lane outcome, not goal completion.
Report: write evidence/IMPL-report.md with the lane name, exact command, committed SHA, raw receipt path and verbatim PASS/FAIL markers, then run `br --db /home/jleechan/.local/state/dark-factory/.beads/beads.db --no-auto-flush --no-auto-import comments add dark-factory-c4zhq "$(cat /home/jleechan/roadmap/af-first-ready-20260913/evidence/IMPL-report.md)"`. Never include credentials. Only the independent verifier closes the subtask after reproducing its criteria.

| Criterion | Check command | External anchor | Independent verifier |
|---|---|---|---|
| Focused and nearest integration suite pass | `python3 -c 'from pathlib import Path; t=Path("/home/jleechan/roadmap/af-first-ready-20260913/execution-reference.md").read_text(); exec(t.split("## C4-GREEN\n",1)[1].split("```python\n",1)[1].split("```",1)[0])'` | Actual committed test execution or fresh Linux/GitHub state as defined in the reference; index JSON alone is insufficient. | Fresh independent factory Codex reviewer; default FAIL until reproduced. |
| TEST/IMPL ancestry and exclusive ownership hold | `python3 -c 'from pathlib import Path; t=Path("/home/jleechan/roadmap/af-first-ready-20260913/execution-reference.md").read_text(); exec(t.split("## OWNERSHIP\n",1)[1].split("```python\n",1)[1].split("```",1)[0])'` | Actual committed test execution or fresh Linux/GitHub state as defined in the reference; index JSON alone is insufficient. | Fresh independent factory Codex reviewer; default FAIL until reproduced. |
| Held identities are unchanged | `python3 -c 'from pathlib import Path; t=Path("/home/jleechan/roadmap/af-first-ready-20260913/execution-reference.md").read_text(); exec(t.split("## HOLDS\n",1)[1].split("```python\n",1)[1].split("```",1)[0])'` | Actual committed test execution or fresh Linux/GitHub state as defined in the reference; index JSON alone is insufficient. | Fresh independent factory Codex reviewer; default FAIL until reproduced. |
