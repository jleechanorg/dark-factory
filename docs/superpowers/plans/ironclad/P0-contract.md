# PREREQ: establish first-ready admission facts

target_repo: jleechanorg/dark-factory
Execution role: administrative subtask of c4zhq; no factory label.

Environment: Mac operator client, /Users/jleechan/projects/worktree_factory_review. All live factory probes and br writes use SSH to jeff-ubuntu. No coding checkout or credential access. The independent admission reviewer runs P0/HOLDS on Linux; collection is not admission.
Goal: collect a reproducible Go adapter/account/bootstrap assessment; create only external evidence under /home/jleechan/roadmap/af-first-ready-20260913/evidence. Dependencies: none. Non-test source budget 0; report target 100 lines. This task can report BLOCKED; it cannot certify readiness by collecting facts.
Reuse: daemon/src/adapters.rs symbols ao_spawn_command, ao_spawn_bridge_path, ChainLlm, run_minimax_judge; daemon/src/tools.rs subprocess boundary; pattern /home/jleechan/roadmap/af-first-ready-20260913/prerequisite-assessment.md.
Steps: 1. Read prerequisite-assessment.md for the known boundaries. Execute exactly `python3 -c 'from pathlib import Path; t=Path("/Users/jleechan/projects/worktree_factory_review/docs/superpowers/plans/ironclad/execution-reference.md").read_text(); exec(t.split("## P0-COLLECT\n",1)[1].split("```python\n",1)[1].split("```",1)[0])'`. The command names every numbered receipt and creates the remote P0-report.md; no manual filename/transfer decision is required. 2. Run the Report command below; report remains BLOCKED and admission FAIL until independent launch/Go reproduction. 3. Only the independent reviewer may produce prerequisites.json and run the P0 and HOLDS admission proofs on Linux, using execution-reference.md's exact invocations. The weak collector does not launch children, repair prerequisites, or write positive admission booleans.
RED: n/a — read-only assessment; pre-state is the exact live service/CLI/source snapshot from step 1.
GREEN: complete assessment plus independently reproduced P0 for admission. A BLOCKED assessment is useful output but is not GREEN.
Do not: do not edit service configuration, AO, wrappers or code; instead identify the exact owner and failing edge. Do not read process environments or .env files; instead retain redacted argv and scope-validation booleans from approved tests. Do not equate br closed status with launch proof; instead reproduce P0.
Prior-failure warning: a permanent persisted park was mislabeled transient; service presence and self-reported green did not establish actual dispatch or account scope.
Stop when: a command returns an unexpected result; retry the identical command once, then report to the parent. Missing artifact, instruction conflict, unsafe/unscoped launch, or unsupported bootstrap means stop the affected lane without edits. Parent diagnoses within the eight-hour execution time-box. No repeated whole-plan review. Default criterion verdict FAIL. A worker reporting failure is a valid lane outcome, not goal completion.
Report: `ssh jeff-ubuntu 'br --db /home/jleechan/.local/state/dark-factory/.beads/beads.db --no-auto-flush --no-auto-import comments add dark-factory-c4zhq "$(cat /home/jleechan/roadmap/af-first-ready-20260913/evidence/P0-report.md)"'`. This references the file created by P0-COLLECT. Only an independent reviewer closes P0 after actual admission proof.

| Criterion | Check command | External anchor | Independent verifier |
|---|---|---|---|
| Admission scope and engine are independently reproduced | `python3 -c 'from pathlib import Path; t=Path("/home/jleechan/roadmap/af-first-ready-20260913/execution-reference.md").read_text(); exec(t.split("## P0\n",1)[1].split("```python\n",1)[1].split("```",1)[0])'` | Actual committed test execution or fresh Linux/GitHub state as defined in the reference; index JSON alone is insufficient. | Fresh independent factory Codex reviewer; default FAIL until reproduced. |
| Held identities are unchanged | `python3 -c 'from pathlib import Path; t=Path("/home/jleechan/roadmap/af-first-ready-20260913/execution-reference.md").read_text(); exec(t.split("## HOLDS\n",1)[1].split("```python\n",1)[1].split("```",1)[0])'` | Actual committed test execution or fresh Linux/GitHub state as defined in the reference; index JSON alone is insufficient. | Fresh independent factory Codex reviewer; default FAIL until reproduced. |
| Selected fallback also rejects invalid scope before spawning | Run P0 using the exact proof invocation above. | Independently reproduced fallback edge, including unavailable primary. | Fresh independent factory reviewer; default FAIL. |
