# EVIDENCE: independently prove c4zhq first READY

target_repo: jleechanorg/dark-factory
Execution role: administrative subtask of c4zhq; no factory label.

Environment: jeff-ubuntu and the final committed c4zhq worker checkout; resolve live PR number from the canonical overlay, never a supplied old URL. Independent factory reviewer only; a different family from the AGY implementing worker. Read secrets only from the approved credential mechanism, never files or copied environment dumps.
Goal: independently reproduce final code, live lifecycle, all eight gates, and unchanged hold identities. Create only evidence receipts under /home/jleechan/roadmap/af-first-ready-20260913/evidence. Dependency: IMPL, resolved from beads.json. Production/test changes 0; report target 100 lines, raw logs excluded.
Reuse: daemon/src/verifier.rs symbol GateReport::to_json and the eight GateName strings; daemon/src/tick.rs GATE_ASSESSMENT and TASK_DISPATCHED; execution-reference.md E2E raw-source checklist.
Steps: 1. Read execution-reference.md in full. Independently rerun P0, C4-GREEN, OWNERSHIP and HOLDS at the final committed head. 2. Query actual AO worker/worktree/commit provenance, live GitHub head/check rollup, canonical SQLite overlay and exact-head gate assessment as specified in E2E. Retain raw outputs; only then write independent-e2e.json. 3. Run E2E, HOLDS and HASH-EVIDENCE. Only after all commands and raw-evidence review pass at the same relevant head/release/state, run CLOSE-EVIDENCE; it resolves the EVIDENCE child ID from beads.json. Never close c4zhq from this lane. Otherwise report the exact failed gate and remain open.
RED: n/a — evidence only. Pre-state E2E must fail before real READY and corroborated exact-head receipts exist.
GREEN: PASS P0, PASS C4-GREEN, PASS OWNERSHIP, PASS E2E and PASS HOLDS with independent raw-source reproduction.
Do not: do not use JSON booleans or READY telemetry alone; instead re-query source systems and retain the independent transcript. Do not label skipped/neutral CI success; instead report every bucket and canonical compensating proof. Do not merge or undraft; instead leave the artifact open and draft.
Prior-failure warning: a service state and self-reported green did not establish actual execution.
Stop when: an unexpected result occurs; retry that command once, then report to the parent. Missing artifacts, conflicting instructions or unsupported/unscoped launch are blockers. Parent diagnoses within eight hours. Default verdict FAIL; worker failure is not goal completion.
Report: write evidence/EVIDENCE-report.md with lane name, exact commands, committed SHA, raw receipt paths and PASS/FAIL lines; run `br --db /home/jleechan/.local/state/dark-factory/.beads/beads.db --no-auto-flush --no-auto-import comments add dark-factory-c4zhq "$(cat /home/jleechan/roadmap/af-first-ready-20260913/evidence/EVIDENCE-report.md)"`. Only the independent verifier runs CLOSE-EVIDENCE after its criteria pass.

| Criterion | Check command | External anchor | Independent verifier |
|---|---|---|---|
| Same committed source and ownership verified | `python3 -c 'from pathlib import Path; t=Path("/home/jleechan/roadmap/af-first-ready-20260913/execution-reference.md").read_text(); exec(t.split("## C4-GREEN\n",1)[1].split("```python\n",1)[1].split("```",1)[0])'` | Actual committed test execution or fresh Linux/GitHub state as defined in the reference; index JSON alone is insufficient. | Fresh independent factory Codex reviewer; default FAIL until reproduced. |
| Actual lifecycle and eight gates independently reproduced | `python3 -c 'from pathlib import Path; t=Path("/home/jleechan/roadmap/af-first-ready-20260913/execution-reference.md").read_text(); exec(t.split("## E2E\n",1)[1].split("```python\n",1)[1].split("```",1)[0])'` | Actual committed test execution or fresh Linux/GitHub state as defined in the reference; index JSON alone is insufficient. | Fresh independent factory Codex reviewer; default FAIL until reproduced. |
| Protected identities remain held | `python3 -c 'from pathlib import Path; t=Path("/home/jleechan/roadmap/af-first-ready-20260913/execution-reference.md").read_text(); exec(t.split("## HOLDS\n",1)[1].split("```python\n",1)[1].split("```",1)[0])'` | Actual committed test execution or fresh Linux/GitHub state as defined in the reference; index JSON alone is insufficient. | Fresh independent factory Codex reviewer; default FAIL until reproduced. |
