# Ponytail lane (lazy-senior-dev ladder: delete/reuse first) — dark-factory cleanup 2026-08-05

Produced by the df-cleanup-review-swarm workflow (run wf_7c58132f-e17), review lane model claude-sonnet-5, verified refute-by-default by independent sonnet skeptics. Goal bead: jleechan-fpnx.

## Lane summary

Applied the ponytail seven-rung ladder as a read-only review lens over worktree_factory_clean_code, focused on "code/files that need not exist." Verified every candidate with direct grep/find evidence across runner/, daemon/, tests/, scripts/, pipelines/, prompts/, docs/, Makefile, install.sh, .github/, pytest.ini before flagging as dead/stale, per the task's mandatory cross-check. Did NOT flag the Healer prefix clustering or _parse_verdict marker regex (sanctioned per CLAUDE.md), did not touch artifacts/repro-developer/* (legitimate git-lfs-backed repro bundle, not a duplicate), did not flag any of the >1000-line runner/*.py files as dead — all are actively imported/referenced (verified via cross-file grep of every runner/*.py module name), so no findings target those beyond noting they're in-scope but clean. Top finding by impact: a live tempdir leak (runner/engine_branches.py:74 mkdtemp against ctx.workdir, combined with tests/test_parallel_fanout.py defaulting workdir to the repo root) has left 378 stray branch_* scratch directories on disk at repo root — a real, reproducible bug with a bounded one-PR fix (test default fix + gitignore hardening + directory cleanup), not just a cosmetic annoyance. Remaining findings are 5 root-level stale artifacts (README.html generated duplicate, 2 superseded per-PR evidence dumps, 1 stale Slack-thread audit doc, 1 leftover session log) all verified zero-reference via repo-wide grep, consistent with the existing docs/cleanup-log.md precedent (bead jleechan-4gx) for this exact class of hygiene cleanup. No git mutations, no edits were performed — this is the read-only findings report only.

## Findings this lane contributed to (post cross-lane dedup)

### 1. Stray root-level debug log committed to repo (`coder.log-delete`)
- **File:** `coder.log` · **Action type:** delete_file · **Risk (as filed):** low
- **Verification class:** CONFIRMED
- **Description:** coder.log (385 bytes, 6 lines) is a raw timestamped transcript of a 2026-06-27 coding session (PR #109: [COMMIT], [PUSH_OK], [WAIT_CI] entries). It is tracked in git, has zero references anywhere in runner/, daemon/, tests/, scripts/, pipelines/, prompts/, docs/, Makefile, install.sh, .github/, or pytest.ini. The only grep match for 'coder.log' in the codebase is an unrelated log-message string in daemon/qw5-pilot-dispatch.sh:101 ('coder log in claude session output'), not a file reference.
- **Proposed action:** git rm coder.log. Optionally add 'coder.log' to .gitignore to prevent future accidental commits of session logs.
- **Verifier verdict:** refuted=False (risk low) — Verified independently: coder.log is git-tracked (added via merge commit ea0091f0), contains only 6 stray timestamped debug lines from a 2026-06-27 session, and a full-repo grep for 'coder.log' outside the file itself returns zero hits except the unrelated log-message string in daemon/qw5-pilot-dispatch.sh:101 ('coder log in claude session output') which is plain English text, not a file path reference. No .gitignore entry exists for it either. Nothing in runner/, daemon/, tests/, pipelines/*.dot, Makefile, install.sh, or .github/ reads or writes this path, so removing it cannot affect imports, CI, or pytest (which doesn't reference it).
- **Verifier safe action:** git rm coder.log (single-file deletion, no other changes needed). Adding a coder.log ignore rule to .gitignore is a reasonable but optional addition — not required for safety.
- **DISPOSITION:** **FIXED** — commit d0cee652

### 2. Stale PR evidence dump at repo root, unreferenced (`evidence-jsby-delete`)
- **File:** `EVIDENCE_jsby.md` · **Action type:** delete_file · **Risk (as filed):** low
- **Verification class:** CONFIRMED
- **Description:** EVIDENCE_jsby.md (9,560 bytes) is a vendor-waiver test-output dump for bead jleechan-jsby (already merged and deployed per MEMORY.md 2026-07-24 'jsby cloud deploy dispatch' entry). It has zero references anywhere in runner/, daemon/, tests/, scripts/, pipelines/, prompts/, docs/, Makefile, install.sh, .github/, or pytest.ini, and does not live under the repo's own evidence/ directory convention (where other evidence artifacts like evidence/smoke-*.log, evidence/x4-live-merge-*/ are organized).
- **Proposed action:** git rm EVIDENCE_jsby.md. The underlying work already landed in PR form; evidence artifacts belong in the PR description/gist per the repo's conventions, not as permanent root-level files.
- **Verifier verdict:** refuted=False (risk low) — Verified independently: EVIDENCE_jsby.md is git-tracked (git ls-files confirms), 9560 bytes, dated Aug 5. grep across runner/, daemon/, tests/, scripts/, pipelines/, prompts/, docs/, Makefile, install.sh, .github/, pytest.ini and broader .py/.dot/.yml/Makefile patterns found zero references anywhere. It documents vendor-waiver test evidence for bead jleechan-jsby, whose commits (0a2c07b7, 7607ef59, 6c0eb01e, 18d3502f) are already merged to history and referenced in MEMORY.md as deployed. It sits outside the repo's evidence/ convention (which is gitignored per .gitignore, confirming evidence/ is meant for ephemeral local bundles, not root-level permanent files like this one). Deleting a single unreferenced markdown file cannot affect pytest collection/imports or CI workflows since nothing reads it.
- **Verifier safe action:** git rm EVIDENCE_jsby.md and commit with a message noting the underlying jsby work is already merged/deployed; no other changes needed.
- **DISPOSITION:** **FIXED** — commit d0cee652

### 3. Stale PR evidence dump at repo root, unreferenced (`evidence-pr387-delete`)
- **File:** `EVIDENCE_PR387.md` · **Action type:** delete_file · **Risk (as filed):** low
- **Verification class:** CONFIRMED
- **Description:** EVIDENCE_PR387.md (1,751 bytes) is a TDD-evidence dump for PR #387 / bead jleechan-ijod (vacuous-test detector). The work was already merged and closed per MEMORY.md ('jleechan-ijod r5 done 2026-07-20 — PR #412 head 175c6ad ... closes issue #387'). It has zero references anywhere in runner/, daemon/, tests/, scripts/, pipelines/, prompts/, docs/, Makefile, install.sh, .github/, or pytest.ini, and like EVIDENCE_jsby.md, sits outside the repo's evidence/ directory convention.
- **Proposed action:** git rm EVIDENCE_PR387.md. The PR it documents is already merged; evidence has no ongoing operational value at repo root.
- **Verifier verdict:** refuted=False (risk low) — Verified independently: grep -rl 'EVIDENCE_PR387' across the whole repo (all file types, not just the finding's listed dirs) returns only the file itself — zero external references in code, tests, CI, docs, or Makefile/install.sh. git log confirms it's an untracked-by-recent-history evidence dump, and its content (PR #387/bead jleechan-ijod TDD pass output) matches MEMORY.md's confirmation that PR #412 already closed issue #387. It sits at repo root outside any evidence/ convention, alongside the similarly-unused EVIDENCE_jsby.md. Deleting a plain markdown file with no inbound references cannot affect pytest collection, imports, CI workflows, or pipeline prompt resolution.
- **Verifier safe action:** git rm EVIDENCE_PR387.md — exactly as proposed; no narrowing needed since verification found zero references and zero risk to the 1483-test baseline.
- **DISPOSITION:** **FIXED** — commit d0cee652

### 4. 378 stray branch_<name>_XXXXXXXX scratch directories at repo root (`branch-scratch-dirs-cleanup`)
- **File:** `.` · **Action type:** delete_file · **Risk (as filed):** low
- **Verification class:** CONFIRMED
- **Description:** Repo root contains 378 empty branch_<branch_name>_<hash> directories (verified: find . -maxdepth 1 -type d -name 'branch_*' | wc -l = 378, all mode 700, all empty). Root cause: runner/engine_branches.py:74 uses tempfile.mkdtemp(prefix=f'branch_{branch_name}_', dir=base) where base=ctx.workdir. tests/test_parallel_fanout.py:26 sets ROOT = Path(__file__).parent.parent (the repo root) as the default workdir in its _ctx() helper used by 35 call sites. Every fan-out test that exercises _branch_context leaks a real tempdir into the repo working tree instead of the OS tmp dir. .gitignore:75 has a narrower branch_fail_step_* pattern proving this class of artifact is excluded elsewhere, but the actual mkdtemp prefix is not covered.
- **Proposed action:** (1) Delete the 378 stray branch_* directories at repo root (untracked, empty, safe). (2) Add branch_[a-z_]*_[a-zA-Z0-9]*/ to .gitignore as defense-in-depth. (3) Fix the leak: change tests/test_parallel_fanout.py's _ctx() default workdir to use a pytest tmp_path fixture instead of ROOT, or have _branch_context fall back to tempfile.gettempdir() in test/CI contexts. Record in docs/cleanup-log.md per existing convention (jleechan-4gx precedent).
- **Verifier verdict:** refuted=False (risk low) — All claims verified directly: 378 stray branch_* dirs existed at repo root (all untracked — `git ls-files | grep '^branch_'` = 0), root cause is confirmed at runner/engine_branches.py:74 (mkdtemp with dir=ctx.workdir) combined with tests/test_parallel_fanout.py's `_ctx()` defaulting `workdir=ROOT` (repo root) across 35 call sites. .gitignore:75 only covers `branch_fail_step_*`, confirmed not to match this prefix. I reproduced the live leak: running just tests/test_parallel_fanout.py took the count from 378 to 433 (+55 in one file run), proving this is an active, ongoing leak, not a one-time artifact. I tested the proposed fix (change ROOT to a real tempdir instead of repo root) — full test_parallel_fanout.py suite still passes 36/36, and the leak stops (0 new dirs). Deletion is safe: no tracked files, nothing referenced by CI/Makefile/install.sh/pipelines.
- **Verifier safe action:** (1) Delete the untracked branch_* scratch dirs at repo root (git ls-files confirms none are tracked). (2) Add a gitignore pattern broad enough to cover the mkdtemp prefix, e.g. `branch_*_*/` near the existing branch_fail_step_* entry. (3) Fix the actual leak source in tests/test_parallel_fanout.py: change `ROOT = Path(__file__).parent.parent` (line 26) to a real tempdir, e.g. `ROOT = Path(tempfile.mkdtemp(prefix="test_parallel_fanout_"))`, since ROOT is used nowhere else in the file except as the `_ctx()` default workdir — verified via grep and a full pytest run (36/36 still pass, and the leak stops: 0 new branch_* dirs after the change vs +55 before). Do not touch runner/engine_branches.py itself; the bug is entirely in the test fixture's choice of default workdir, not in the production leak-fallback logic. Record in docs/cleanup-log.md per convention.
- **DISPOSITION:** **FIXED (root cause)** — commit 7fbaf974 fixes the leak source (test workdir -> OS tempdir) and adds the .gitignore branch_*_*/ guard. The stray dirs themselves were untracked and cleared out-of-band (not part of any commit). Cold-review residual: OTHER test files still create branch_* dirs at repo root during full-suite runs (gitignored, disk-only); sweep tracked in bead jleechan-vxi3.

### 5. Generated README.html is stale relative to README.md (`readme-html-sync`)
- **File:** `README.html` · **Action type:** fix · **Risk (as filed):** low
- **Verification class:** CONFIRMED
- **Description:** README.html is checked in as a generated artifact per diagrams/build_readme_html.py. Running the generator produces a 7-insertion/2-deletion diff against the committed version, indicating README.md was edited but README.html was not regenerated and re-committed. This is a violation of the repo's own documented contract (diagrams/README.md) that the two must stay in sync.
- **Proposed action:** Run `python3 diagrams/build_readme_html.py` and commit the regenerated README.html. If README.md is edited, the generated HTML must be regenerated and committed in the same PR.
- **Verifier verdict:** refuted=False (risk low) — Verified independently: running `python3 diagrams/build_readme_html.py` produces a genuine +7/-2 diff against the committed README.html, and it's not cosmetic noise — README.md has a real section (`### 🎯 The Ultimate Goal: Deprecating the Human Interactive Hat`, README.md:58) that is entirely absent from the committed README.html. The generator (diagrams/build_readme_html.py) has no timestamps or nondeterministic output, so re-running it produces a stable, reviewable diff. Grep confirms README.html is not referenced by any .github/workflows/*, Makefile, install.sh, pipelines/*.dot, or tests/ — it's a standalone doc artifact, so regenerating and committing it cannot affect CI, imports, or pytest (which stays green regardless, since no test touches this file).
- **Verifier safe action:** Run `python3 diagrams/build_readme_html.py` and commit the regenerated README.html as a docs-only change in this cleanup PR (verify via `git diff --stat README.html` that the diff is limited to content additions matching README.md's new section, with no unrelated churn).
- **DISPOSITION:** **FIXED** — commit 042a2463 (regenerated via diagrams/build_readme_html.py)

### 6. One-off Slack audit doc at repo root should be moved or deleted (`parallelization-audit-relocate`)
- **File:** `PARALLELIZATION_AUDIT_2026-06-10.md` · **Action type:** refactor · **Risk (as filed):** low
- **Verification class:** CONFIRMED
- **Description:** PARALLELIZATION_AUDIT_2026-06-10.md (5,725 bytes) is a Hermes-authored Slack-thread audit from 2026-06-10 about a historical worldarchitect.ai incident. It is referenced only from docs/factory-evolve-research/convo-history-forensics.md (3 citations, using a stale absolute path '/Users/jleechan/projects/dark-factory/PARALLELIZATION_AUDIT_2026-06-10.md'), which is a historical-forensics doc, not a functional consumer. The file sits at repo root instead of under docs/ where other dated audit docs (docs/cli-fallback-audit-2026-06-12.md, docs/factory-goal-gap-review-2026-07-06.md) are organized.
- **Proposed action:** Either move PARALLELIZATION_AUDIT_2026-06-10.md into docs/factory-evolve-research/ and fix the stale absolute-path citations in convo-history-forensics.md to relative links, or delete it and convert the three citations to inline quotes if the historical detail must be preserved. Minimum action: remove from repo root to reduce root-level clutter and follow the established directory convention.
- **Verifier verdict:** refuted=False (risk low) — Verified via grep: PARALLELIZATION_AUDIT_2026-06-10.md has zero references in runner/, daemon/, tests/, pipelines/*.dot, Makefile, install.sh, or .github/workflows/*.yml. The only references are 3 citations in docs/factory-evolve-research/convo-history-forensics.md, and those already use a stale absolute macOS path (/Users/jleechan/projects/dark-factory/...) that is broken today regardless of where the file lives — so relocating it introduces no new breakage. It genuinely sits at root while sibling dated audit docs (docs/cli-fallback-audit-2026-06-12.md) live under docs/. Baseline pytest is unaffected since nothing imports or executes this markdown file.
- **Verifier safe action:** git mv PARALLELIZATION_AUDIT_2026-06-10.md docs/factory-evolve-research/PARALLELIZATION_AUDIT_2026-06-10.md, then in docs/factory-evolve-research/convo-history-forensics.md replace the 3 stale absolute-path citations (/Users/jleechan/projects/dark-factory/PARALLELIZATION_AUDIT_2026-06-10.md) with the relative filename PARALLELIZATION_AUDIT_2026-06-10.md (same directory, no path needed). Do not delete the file or rewrite citations into inline quotes — that's unnecessary content loss beyond a pure relocation. Skip if this cleanup PR is meant to stay narrowly scoped to higher-value items; this one is safe but low-payoff (single file move) and could be deferred to a follow-up doc-hygiene pass instead.
- **DISPOSITION:** **FIXED** — commit 58fb9c54 (git mv + citation link repair)
