# Thermo lane (structural/maintainability rubric) — dark-factory cleanup 2026-08-05

Produced by the df-cleanup-review-swarm workflow (run wf_7c58132f-e17), review lane model claude-sonnet-5, verified refute-by-default by independent sonnet skeptics. Goal bead: jleechan-fpnx.

## Lane summary

Structure lane on worktree_factory_clean_code (dark-factory, origin/main 9159a777), read-only, no mutations performed. Verified the historically-flagged runner/handler_parallel_reviewer <-> runner/handlers circular import is already resolved (confirmed via tests/test_handler_parallel_reviewer_imports.py passing standalone with `pytest tests/test_handler_parallel_reviewer_imports.py` and a TYPE_CHECKING-only cross-reference in handler_parallel_reviewer.py) — not reported as a finding since it's a non-issue. Top structural findings, ranked by payoff/diff-size: (1) skeptic_gate.py's 2336 lines contain a cleanly-bounded ~750-line contract-echo subsystem (verified only 6 external referrers, all in skeptic_gate_cli.py/tests) that can move to its own module with a re-export shim — the single highest-value, lowest-risk extraction found. (2) handler_codergen.py duplicates ~250 lines of shadow-review subprocess/timeout/sidecar logic that handler_dispatch.py already implements more generally (and which handler_parallel_reviewer.py already consumes) — a genuine copy-paste-drift consolidation target. (3) engine_run.py's run() and skeptic_gate_cli.py's main() are each single functions spanning 60-80% of their files; proposed one bounded, low-risk sub-block extraction each rather than a rewrite. (4) Five root-level stray files (coder.log, EVIDENCE_jsby.md, EVIDENCE_PR387.md, PARALLELIZATION_AUDIT_2026-06-10.md) are one-off historical PR/incident artifacts with zero code/test/doc references (verified via repo-wide grep across runner/, daemon/, tests/, scripts/, pipelines/, prompts/, docs/, Makefile, install.sh, .github/, pytest.ini) — safe deletions that reduce root clutter. Explicitly did NOT flag README.html (it is a build artifact generated by diagrams/build_readme_html.py and linked from README.md — legitimate) nor the Healer prefix-clustering or _parse_verdict marker-regex logic (both are documented internal contracts per CLAUDE.md, not violations). All findings are independently implementable inside one bounded cleanup PR."

## Findings this lane contributed to (post cross-lane dedup)

### 1. Stray root-level debug log committed to repo (`coder.log-delete`)
- **File:** `coder.log` · **Action type:** delete_file · **Risk (as filed):** low
- **Verification class:** CONFIRMED
- **Description:** coder.log (385 bytes, 6 lines) is a raw timestamped transcript of a 2026-06-27 coding session (PR #109: [COMMIT], [PUSH_OK], [WAIT_CI] entries). It is tracked in git, has zero references anywhere in runner/, daemon/, tests/, scripts/, pipelines/, prompts/, docs/, Makefile, install.sh, .github/, or pytest.ini. The only grep match for 'coder.log' in the codebase is an unrelated log-message string in daemon/qw5-pilot-dispatch.sh:101 ('coder log in claude session output'), not a file reference.
- **Proposed action:** git rm coder.log. Optionally add 'coder.log' to .gitignore to prevent future accidental commits of session logs.
- **Verifier verdict:** refuted=False (risk low) — Verified independently: coder.log is git-tracked (added via merge commit ea0091f0), contains only 6 stray timestamped debug lines from a 2026-06-27 session, and a full-repo grep for 'coder.log' outside the file itself returns zero hits except the unrelated log-message string in daemon/qw5-pilot-dispatch.sh:101 ('coder log in claude session output') which is plain English text, not a file path reference. No .gitignore entry exists for it either. Nothing in runner/, daemon/, tests/, pipelines/*.dot, Makefile, install.sh, or .github/ reads or writes this path, so removing it cannot affect imports, CI, or pytest (which doesn't reference it).
- **Verifier safe action:** git rm coder.log (single-file deletion, no other changes needed). Adding a coder.log ignore rule to .gitignore is a reasonable but optional addition — not required for safety.
- **DISPOSITION:** **FIXED** — commit d0cee652

### 2. Stale PR evidence dump at repo root, unreferenced (`evidence-jsby-delete`)
- **File:** `EVIDENCE_jsby.md` · **Action type:** delete_file · **Risk (as filed):** low
- **Verification class:** CONFIRMED
- **Description:** EVIDENCE_jsby.md (9,560 bytes) is a vendor-waiver test-output dump for bead jleechan-jsby (already merged and deployed per MEMORY.md 2026-07-24 'jsby cloud deploy dispatch' entry). It has zero references anywhere in runner/, daemon/, tests/, scripts/, pipelines/, prompts/, docs/, Makefile, install.sh, .github/, or pytest.ini, and does not live under the repo's own evidence/ directory convention (where other evidence artifacts like evidence/smoke-*.log, evidence/x4-live-merge-*/ are organized).
- **Proposed action:** git rm EVIDENCE_jsby.md. The underlying work already landed in PR form; evidence artifacts belong in the PR description/gist per the repo's conventions, not as permanent root-level files.
- **Verifier verdict:** refuted=False (risk low) — Verified independently: EVIDENCE_jsby.md is git-tracked (git ls-files confirms), 9560 bytes, dated Aug 5. grep across runner/, daemon/, tests/, scripts/, pipelines/, prompts/, docs/, Makefile, install.sh, .github/, pytest.ini and broader .py/.dot/.yml/Makefile patterns found zero references anywhere. It documents vendor-waiver test evidence for bead jleechan-jsby, whose commits (0a2c07b7, 7607ef59, 6c0eb01e, 18d3502f) are already merged to history and referenced in MEMORY.md as deployed. It sits outside the repo's evidence/ convention (which is gitignored per .gitignore, confirming evidence/ is meant for ephemeral local bundles, not root-level permanent files like this one). Deleting a single unreferenced markdown file cannot affect pytest collection/imports or CI workflows since nothing reads it.
- **Verifier safe action:** git rm EVIDENCE_jsby.md and commit with a message noting the underlying jsby work is already merged/deployed; no other changes needed.
- **DISPOSITION:** **FIXED** — commit d0cee652

### 3. Stale PR evidence dump at repo root, unreferenced (`evidence-pr387-delete`)
- **File:** `EVIDENCE_PR387.md` · **Action type:** delete_file · **Risk (as filed):** low
- **Verification class:** CONFIRMED
- **Description:** EVIDENCE_PR387.md (1,751 bytes) is a TDD-evidence dump for PR #387 / bead jleechan-ijod (vacuous-test detector). The work was already merged and closed per MEMORY.md ('jleechan-ijod r5 done 2026-07-20 — PR #412 head 175c6ad ... closes issue #387'). It has zero references anywhere in runner/, daemon/, tests/, scripts/, pipelines/, prompts/, docs/, Makefile, install.sh, .github/, or pytest.ini, and like EVIDENCE_jsby.md, sits outside the repo's evidence/ directory convention.
- **Proposed action:** git rm EVIDENCE_PR387.md. The PR it documents is already merged; evidence has no ongoing operational value at repo root.
- **Verifier verdict:** refuted=False (risk low) — Verified independently: grep -rl 'EVIDENCE_PR387' across the whole repo (all file types, not just the finding's listed dirs) returns only the file itself — zero external references in code, tests, CI, docs, or Makefile/install.sh. git log confirms it's an untracked-by-recent-history evidence dump, and its content (PR #387/bead jleechan-ijod TDD pass output) matches MEMORY.md's confirmation that PR #412 already closed issue #387. It sits at repo root outside any evidence/ convention, alongside the similarly-unused EVIDENCE_jsby.md. Deleting a plain markdown file with no inbound references cannot affect pytest collection, imports, CI workflows, or pipeline prompt resolution.
- **Verifier safe action:** git rm EVIDENCE_PR387.md — exactly as proposed; no narrowing needed since verification found zero references and zero risk to the 1483-test baseline.
- **DISPOSITION:** **FIXED** — commit d0cee652

### 4. One-off Slack audit doc at repo root should be moved or deleted (`parallelization-audit-relocate`)
- **File:** `PARALLELIZATION_AUDIT_2026-06-10.md` · **Action type:** refactor · **Risk (as filed):** low
- **Verification class:** CONFIRMED
- **Description:** PARALLELIZATION_AUDIT_2026-06-10.md (5,725 bytes) is a Hermes-authored Slack-thread audit from 2026-06-10 about a historical worldarchitect.ai incident. It is referenced only from docs/factory-evolve-research/convo-history-forensics.md (3 citations, using a stale absolute path '/Users/jleechan/projects/dark-factory/PARALLELIZATION_AUDIT_2026-06-10.md'), which is a historical-forensics doc, not a functional consumer. The file sits at repo root instead of under docs/ where other dated audit docs (docs/cli-fallback-audit-2026-06-12.md, docs/factory-goal-gap-review-2026-07-06.md) are organized.
- **Proposed action:** Either move PARALLELIZATION_AUDIT_2026-06-10.md into docs/factory-evolve-research/ and fix the stale absolute-path citations in convo-history-forensics.md to relative links, or delete it and convert the three citations to inline quotes if the historical detail must be preserved. Minimum action: remove from repo root to reduce root-level clutter and follow the established directory convention.
- **Verifier verdict:** refuted=False (risk low) — Verified via grep: PARALLELIZATION_AUDIT_2026-06-10.md has zero references in runner/, daemon/, tests/, pipelines/*.dot, Makefile, install.sh, or .github/workflows/*.yml. The only references are 3 citations in docs/factory-evolve-research/convo-history-forensics.md, and those already use a stale absolute macOS path (/Users/jleechan/projects/dark-factory/...) that is broken today regardless of where the file lives — so relocating it introduces no new breakage. It genuinely sits at root while sibling dated audit docs (docs/cli-fallback-audit-2026-06-12.md) live under docs/. Baseline pytest is unaffected since nothing imports or executes this markdown file.
- **Verifier safe action:** git mv PARALLELIZATION_AUDIT_2026-06-10.md docs/factory-evolve-research/PARALLELIZATION_AUDIT_2026-06-10.md, then in docs/factory-evolve-research/convo-history-forensics.md replace the 3 stale absolute-path citations (/Users/jleechan/projects/dark-factory/PARALLELIZATION_AUDIT_2026-06-10.md) with the relative filename PARALLELIZATION_AUDIT_2026-06-10.md (same directory, no path needed). Do not delete the file or rewrite citations into inline quotes — that's unnecessary content loss beyond a pure relocation. Skip if this cleanup PR is meant to stay narrowly scoped to higher-value items; this one is safe but low-payoff (single file move) and could be deferred to a follow-up doc-hygiene pass instead.
- **DISPOSITION:** **FIXED** — commit 58fb9c54 (git mv + citation link repair)

### 5. Extract bead-contract/contract-echo subsystem into its own module (`skeptic_gate-extract-contract-echo`)
- **File:** `runner/skeptic_gate.py` · **Action type:** refactor · **Risk (as filed):** medium
- **Verification class:** REFUTED-BY-VERIFIER
- **Description:** skeptic_gate.py is 2,336 lines. Lines ~283–1,035 (AcceptanceItem, PriorFinding, BeadContract, ContractEchoItem, ContractEchoReport, ContractEchoVerdictResult, PriorFindingEcho, load_bead_contract, _br_show_json, load_bead_contract_from_bead, _strip_contract_echo_block, parse_contract_echo, evaluate_contract_echo) form a self-contained contract-echo concern (~750 lines) marked by the file's own '# --- Parsing ---' comment at line 1036. This block has only 6 external referrers (runner/skeptic_gate_cli.py + 5 test files under tests/test_skeptic_contract_echo*.py, tests/test_skeptic_gate_cli_*), none in daemon/ or pipelines/.
- **Proposed action:** Move lines ~283–1,035 into a new runner/skeptic_contract_echo.py module with the contract-echo dataclasses and functions. Re-export the same names from runner/skeptic_gate.py (from .skeptic_contract_echo import * or explicit re-import list) so existing call sites in skeptic_gate_cli.py and tests keep working unchanged. Reduces skeptic_gate.py by ~750 lines with zero behavior change.
- **Verifier verdict:** refuted=True (risk medium) — The line-range/referrer evidence checks out (AcceptanceItem@283 through evaluate_contract_echo@909, module marker comment at ~1035-1037, and exactly runner/skeptic_gate_cli.py + 5 test files reference these symbols outside skeptic_gate.py — verified by grep). However, the proposed mechanism (\"re-export the same names ... from runner/skeptic_gate.py\") is unsafe as written: `load_bead_contract_from_bead` internally calls `_br_show_json` via a bare module-global name, and `tests/test_skeptic_contract_echo_r3.py` does `monkeypatch.setattr(skeptic_gate, \"_br_show_json\", fake_show)` then calls `skeptic_gate.load_bead_contract_from_bead(...)`. I reproduced this exact pattern standalone (moving both functions into one module and re-exporting into a stand-in for skeptic_gate, then monkeypatching the re-exported attribute): the monkeypatch is a no-op because the moved function's internal call resolves against its own module's globals, not the re-exported copy. Confirmed by a runnable repro — patched value never reaches the call, real function runs instead. That means this test would either fail or fall through to a live `br show --json ...` subprocess call, i.e. the refactor is NOT \"zero behavior change\" as claimed and risks turning the currently-green 1483-test baseline red for the wrong reason (subprocess/br-binary dependency leaking into a unit test).
- **Verifier safe action:** Do not do a blanket "move + re-export" of the block. If this extraction is pursued at all, it must (a) move the whole contract-echo block including all of its internal call graph together, (b) rewrite `tests/test_skeptic_contract_echo_r3.py`'s `monkeypatch.setattr(skeptic_gate, \"_br_show_json\", ...)` to target the new module (`skeptic_contract_echo._br_show_json`) explicitly, and (c) run `tests/test_skeptic_contract_echo_r3.py` (and the other 4 referrer test files) green post-move before considering it done — a plain `from .skeptic_contract_echo import *` re-export is not sufficient. Given the medium risk and value=2 payoff already self-assessed by the finding, this is better scoped as its own dedicated PR with the test-file updates included, not folded into this general cleanup PR unencumbered.
- **DISPOSITION:** **REJECTED** — verifier: move+re-export mechanism unsafe (monkeypatch targets, import graph); follow-up bead jleechan-t5ld

### 6. run() is a single 890-line function; extract checkpoint-restore block (`engine_run-extract-checkpoint-block`)
- **File:** `runner/engine_run.py` · **Action type:** refactor · **Risk (as filed):** medium
- **Verification class:** REFUTED-BY-VERIFIER
- **Description:** runner/engine_run.py:213 def run(...) extends to end of file (~line 1102) — roughly 890 lines, ~80% of the file's line count, mixing graph stepping, checkpoint loading, parallel fan-out, and step dispatch in one function body. The checkpoint-resume block alone (lines ~236–290: loading resumed history, recomputing visits, detecting exit-node/max_steps early-return cases, incomplete-parallel-fanout repair) is a distinct, independently testable concern currently inlined into main control flow.
- **Proposed action:** Extract the resume/checkpoint-restoration block (lines ~236–290) into a private helper function e.g. `_restore_from_checkpoint(graph, resume, max_steps) -> tuple[list, dict, int, bool]` called once at the top of run(). This is a pure refactor extraction (no behavior change) that trims run() by ~55 lines and gives checkpoint-resume logic its own unit-testable seam separate from main step loop.
- **Verifier verdict:** refuted=True (risk medium) — The finding is factually right about line count (890 lines, run() spans 213-1102) but wrong that the checkpoint-resume block is a clean, low-risk extraction. The repo's own docs/refactor/file-ownership-map.engine.md already analyzed this exact function: it labels the resume+checkpoint section (L1027-1066 in the old numbering) as the first sub-block of the main loop and explicitly states 'do not split mid-loop' for the surrounding sections, and separately says splitting run() 'would require passing 12+ state locals as parameters' and calls the loop 'one inseparable unit' (line 179 of that doc). The block mutates/reads `history`, `visits`, `current`, and needs two different early-return sentinels (checkpoint-at-exit-node, checkpoint-past-max_steps) plus a full Node object for `current` — the proposed signature `tuple[list, dict, int, bool]` doesn't even model returning a Node, showing the extraction wasn't actually attempted before being proposed. Combined with CLAUDE.md's explicit framing of runner/ as disposable 'dorodango' code (polish/discard/rebuild from spec, not a durable asset) and the finding's own value=2 (lowest tier), a ~55-line mechanical extraction with real coupling risk on a file already flagged as over-budget-by-design doesn't clear the bar for this cleanup PR.
- **Verifier safe action:** Do not extract the checkpoint-resume block. If any action is taken, limit it to a documentation-only update: refresh docs/refactor/file-ownership-map.engine.md's line-count note for runner/engine_run.py (890 lines now vs the ~750 line estimate recorded there) — no production code change.
- **DISPOSITION:** **REJECTED** — repo's own docs/refactor/file-ownership-map.engine.md marks the block do-not-split; optional line-count doc refresh skipped as churn

### 7. main() is a single ~640-line function; extract prologue or epilogue (`skeptic_gate_cli-extract-main-prologue`)
- **File:** `runner/skeptic_gate_cli.py` · **Action type:** refactor · **Risk (as filed):** medium
- **Verification class:** REFUTED-BY-VERIFIER
- **Description:** runner/skeptic_gate_cli.py:720 def main(argv=None) extends to line 1363 — roughly 640 lines, ~40% of the 1542-line file, mixing argument parsing, gh API orchestration, comment posting, retry/backoff, and exit-code decisions in one function body.
- **Proposed action:** Identify one clearly separable sub-phase inside main() (e.g. the argument-parsing-and-validation prologue before the first gh_api call, or the final status-reporting/exit-code epilogue) and extract it into a helper function with an explicit return contract, following the same low-risk extraction pattern as the engine_run.py finding. Do this incrementally (one bounded extraction per PR) rather than a full main() decomposition.
- **Verifier verdict:** refuted=True (risk medium) — The size claim is accurate (main() does span 720-1362, ~643 lines, confirmed via grep) but the proposed action is not a concrete bounded diff — it asks the executor to \"identify one clearly separable sub-phase\" themselves inside a function that interleaves argparse, gh API calls, retry/backoff, and exit-code decisions across many early-return paths, with no specified extraction boundary or return contract. That is exactly the shape of change likely to alter control flow or shared-local semantics in a way unit tests (which exercise helper functions individually, not main()'s full flow) wouldn't catch. The finding's own stated value is low (2) and it explicitly disclaims being a full decomposition, i.e. it admits this is a small, non-terminal, judgment-heavy step — not worth the medium risk in a cleanup PR.
- **Verifier safe action:** Do not include this finding in the cleanup PR as scoped. If maintainability work on this file is desired, open a tracking bead describing the specific sub-phase to extract with an exact line range and return-contract, then do that extraction in its own small PR with a dedicated before/after unit test asserting behavior parity — not as part of a general cleanup sweep.
- **DISPOSITION:** **REJECTED** — verifier: no concrete bounded diff; follow-up bead jleechan-f1a2

### 8. Consolidate duplicated shadow-review subprocess logic with handler_dispatch.py (`handler_codergen-dedup-shadow-review`)
- **File:** `runner/handler_codergen.py` · **Action type:** refactor · **Risk (as filed):** medium
- **Verification class:** VOID (verifier died; re-verified out-of-band)
- **Description:** handler_codergen.py implements its own codex-only shadow-review spawn/wait/parse pipeline (_ShadowCodexReview, _shadow_review_enabled, _shadow_review_prompt, _start_shadow_codex_review lines 185–255, _finish_shadow_codex_review lines 257–380 — about 250 lines total) that duplicates the same subprocess-launch-with-sidecar / timeout-wait-with-SIGTERM-then-SIGKILL / verdict-parse / metadata-merge pattern already implemented more generally in runner/handler_dispatch.py's _launch_shadow_gate_review (lines 157–263) and _finish_shadow_gate_review (lines 294–450), which already supports backend='codex' as its default and is imported directly by handler_parallel_reviewer.py. The codergen.py copy is a simpler, older, codex-only fork of the same logic.
- **Proposed action:** Replace handler_codergen.py's _start_shadow_codex_review/_finish_shadow_codex_review bodies with thin wrappers that call handler_dispatch._launch_shadow_gate_review(..., backend='codex') / _finish_shadow_gate_review(...), keeping the codergen-specific metadata key names (shadow_codex_* vs shadow_{backend}_gate_*) at the call site only. Removes ~200 lines of duplicated process-management code; handler_dispatch.py already exports the more general functions and handler_parallel_reviewer.py already depends on them.
- **DISPOSITION:** **REJECTED for this PR** — adversarial re-verify (sonnet, fresh agent): risk HIGH — sandbox-scope, expected_sha, and kill-escalation semantics differ; follow-up bead jleechan-txdh
