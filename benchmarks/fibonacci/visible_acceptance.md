# Visible acceptance

The implementation is accepted when:

- `python fib.py <n>` works from the candidate directory.
- Public examples in `spec.md` pass.
- Invalid arguments fail closed with stderr.
- The solution is deterministic and offline.

Exact hidden scenario values and evaluator implementation details are not part
of the coder prompt.

