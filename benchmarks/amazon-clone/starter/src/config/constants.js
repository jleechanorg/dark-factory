const DEFAULT_HOST = process.env.HOST || '127.0.0.1';
const DEFAULT_PORT = process.env.PORT || '3000';

const FIRESTORE_EMULATOR_HOST =
  process.env.FIRESTORE_EMULATOR_HOST ||
  process.env.FIRESTORE_EMULATOR_HOSTNAME ||
  '127.0.0.1:8080';

const FIREBASE_PROJECT_ID =
  process.env.GOOGLE_CLOUD_PROJECT ||
  process.env.GCLOUD_PROJECT ||
  'amazon-clone-local';

const FIREBASE_EMULATOR_UI_HOST = process.env.FIREBASE_EMULATOR_UI_HOST || '127.0.0.1';
const FIREBASE_EMULATOR_UI_PORT = process.env.FIREBASE_EMULATOR_UI_PORT || '4000';

const SESSION_COOKIE_NAME = 'amazon_session';
const SESSION_DURATION_MS = 1000 * 60 * 60 * 24 * 14;

module.exports = {
  DEFAULT_HOST,
  DEFAULT_PORT,
  FIRESTORE_EMULATOR_HOST,
  FIREBASE_PROJECT_ID,
  FIREBASE_EMULATOR_UI_HOST,
  FIREBASE_EMULATOR_UI_PORT,
  SESSION_COOKIE_NAME,
  SESSION_DURATION_MS,
};
