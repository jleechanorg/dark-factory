# Attractor Spec Review Starter

This benchmark’s starter is a small scaffold for running the validator implementation.

Expected implementation points:

- `scripts/validate_spec.py` — core line-aware validator.
- `scripts/fullstack_smoke.sh` — required for full-pipeline smoke.
- `backend/main.py`, `frontend/index.html`, `firestore.rules` — full pipeline artifacts.
