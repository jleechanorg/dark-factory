Create a concise implementation plan for this spec validation benchmark.

Goal:
${goal}

Read:
- `spec/feature.md`
- `visible_acceptance.md`

Output:
- A compact plan with:
  - validation schema you will produce,
  - likely edge cases,
  - file-by-file implementation tasks,
  - acceptance checklist.

Rules:
- Keep this as a real implementation plan, not implementation.
- Mention full-stack artifacts only if required by `full` method:
  `backend/main.py`, `frontend/index.html`, `firestore.rules`.
