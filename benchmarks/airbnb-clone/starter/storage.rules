rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Sprint implementations replace this deny-all baseline with storage object rules.
      allow read, write: if false;
    }
  }
}
