Starter scaffold for Sprint 1 implementation.
Run `pnpm install` then `firebase emulators:start`.
Copy `.env.local.example` to `.env.local` and fill in values.
See `../spec.md` for the full feature specification.
Do not add business logic to this scaffold.
