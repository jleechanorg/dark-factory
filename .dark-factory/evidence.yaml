# Scratch + optional gate-audit aliases only.
# Operator policy for operator_verify is pinned from git history at:
#   .github/dark-factory-operator.yaml   (preferred)
# Legacy fallback (deprecated):
#   .dark-factory/evidence.yaml
