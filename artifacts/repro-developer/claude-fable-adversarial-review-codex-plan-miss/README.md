# Repro Developer Artifact: claude-fable-adversarial-review-codex-plan-miss

This directory is intended for Git LFS tracking.

- `*-sanitized.tar.*`: gitleaks-clean sanitized repro for normal repo transport.
- `*.tar.*.gpg`: exact raw repro archive encrypted with GPG symmetric encryption.
- Do not commit the passphrase file. Share the passphrase only out of band with the intended engineer.

Raw passphrase file on this machine: `/tmp/claude-fable-adversarial-review-codex-plan-miss.tar.zst.passphrase.txt`
