# Fix Failures: smoke

The holdout evaluator returned failure. You do NOT have access to the holdout
scenarios. Inspect the spec at `specs/hello.md` again, re-read the code at
`impl/greet.py`, and correct any divergence from the spec contract.

## Rules

- You have full read-write tool access to the current workspace. Use your tools to inspect, build, run tests, and verify the repository state as needed.
