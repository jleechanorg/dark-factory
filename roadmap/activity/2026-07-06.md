# Activity — 2026-07-06

- **Auto-factory alignment and bug fixes (this run)**:
  - Resolved git rebase conflicts on local `main` and pushed the default-coder changes cleanly to `origin/main`. The default coder configuration now uses AO with `antigravity` agent by default, falling back to `agy` then `claude` on failures.
  - Investigated [PR #7888](https://github.com/jleechanorg/worldarchitect.ai/pull/7888) check failures and resolved the duplicate `rev-zzw3` bead ID conflict in `.beads/issues.jsonl` in the PR worktree. Committed and pushed the fix to the remote branch.
  - Fixed a critical verifier bug where CodeRabbit review states were parsed as `unknown` when the last review was a comment (`COMMENTED`). Added filtering in both `daemon/src/adapters.rs` and `daemon/src/gates_compute.rs` to ignore `COMMENTED` reviews.
  - Verified that all 110 cargo unit and integration tests compile and pass green.

- **Situational Assessment and exit criteria check**:
  - Four out of the six main candidate PRs have successfully **MERGED**: `worldarchitect.ai` PR #8058, PR #8116, PR #8064, and PR #8061, plus `dark-factory` PR #133.
  - PR #7888 and PR #8060 remain **OPEN** and are currently undergoing re-testing/GHA check runs.
  - Authored [Nextsteps 2026-07-06](nextsteps-2026-07-06.md) summarizing the current bead index, work queue, and next actions.

- **Late evening — PR #8189 GHA fallback + quota-blocked dispatch (this run)**:
  - Opened and pushed [PR #8189](https://github.com/jleechanorg/worldarchitect.ai/pull/8189) (`fix/GHA-ensurepip-break-system-packages-fallback`, head `3447e85a`, MERGEABLE). Three commits: initial `--break-system-packages` support in `scripts/venv_utils.sh`, partial-venv cleanup before fallback, and robust Node executable lookup in `mvp_site/tests/test_app_auth_persistence_ordering.py` (cross-user NVM paths).
  - Also committed a Dockerfile fix (`mvp_site/Dockerfile`) that creates the `nonroot` group + user when building on the Debian-based `python:3.11-slim` base image (was missing the group add).
  - **Quota-blocked:** MiniMax individual quota hit during prior session (resets ~22:30Z); no new AO dispatches for #8060 or #7888 in this pass.
  - Authored **late-evening addendum** to [nextsteps-2026-07-06-auto-factory-intake-callpath.md](nextsteps-2026-07-06-auto-factory-intake-callpath.md) capturing the PR #8189 work, stranded AO workers, and the uncommitted-files blocker.

- **Late night — Factory merge wrap-up + launchd install (this run)**:
  - Merged PR [#168](https://github.com/jleechanorg/dark-factory/pull/168)–[#171](https://github.com/jleechanorg/dark-factory/pull/171) stack onto `origin/main` via local merge + push (`72b5087c1`). GitHub shows CLOSED/`mergedAt=null`; code is on `main`.
  - Installed `ai.dark-factory.af-tick` launchd agent (240s poll). Agent exit code 1: `FileNotFoundError: br` in `af-tick.err.log` — launchd bare PATH; [#172](https://github.com/jleechanorg/dark-factory/pull/172) wrapper fix still CONFLICTING.
  - Tests green locally: `test_factory_af_tick.sh` 21/21, callpath harness PASS, `cargo test` ok.
  - Beads: created jleechan-v2wv, jleechan-ebe1, jleechan-t4m8; closed jleechan-38w8, jleechan-a5p.
  - Nextsteps: [`roadmap/nextsteps-2026-07-06-launchd-ci-fixes.md`](nextsteps-2026-07-06-launchd-ci-fixes.md).

- **Evening — Launchd installer relocation + CI bash-tests + callpath probe vendoring (this run)**:
  - User asked for "review this work readonly" on 4 PRs (#168 launchd installer, #169 smoke-test evidence, #170 callpath probe, #171 factory-af-tick refactor). Delivered full readonly review with 17+ findings via 4 parallel Explore agents + own analysis.
  - User pivoted to "move installer to repo root and fix an issue if a bug reintroduced", then "do it all". Executed:
    1. **PR [#172](https://github.com/jleechanorg/dark-factory/pull/172)** — Moved `daemon/launchd/install-launchagents.sh` → `./install-launchagents.sh`; added `daemon/launchd/launchd-wrapper.sh` to source `~/.bash_profile` (launchd's bare PATH can't find br/gh/sqlite3/python3/callpath); added `ThrottleInterval=60` for crash-loop protection; parameterized `StartInterval` via `AFD_TICK_INTERVAL_SEC` env var; `chmod 0644` on rendered plist (survives `umask 002`); `--label <name>` repeatable flag; `validate_template_placeholders` rejects non-installer placeholders (qw5-pilot-dispatch.plist.template uses @YEAR@/@MONTH@/...). Single-pass arg parser without `${!i}` indirect-expansion bug. Commit `5905db9a9`.
    2. **PR [#173](https://github.com/jleechanorg/dark-factory/pull/173)** — Vendored `bin/overlay-harness-check.sh` (portable copy of `overlay_harness_check()` from user-scope callpath profile); rewrote `tests/scripts/test_callpath_overlay_harness.sh` to call vendored probe (no user-scope dep); added "Run bash integration tests" step to `.github/workflows/ci.yml` (iterates `tests/scripts/test_*.sh`); added "Fetch all branches" step (`git fetch --no-tags --depth=1 origin main:main`) before `cargo test` to resolve `test_cli_vcs_real_git` panic. Commit `5dc86e1e2`.
  - Closed all 4 originals: **#168** superseded by #172; **#169** stale base + CI now covers regression; **#170** user-scope dep eliminated by #173 vendoring; **#171** superseded by `0368a93e5` which re-introduced 4 silent failure modes.
  - **9 beads filed, 9 closed** during this session. **4 P1 beads remain open** as follow-ups to `0368a93e5`'s direct-UPDATE path: `jleechan-2r1k` (silent failure modes), `jleechan-q47c` (ZFC stderr keyword routing), `jleechan-81wa` (hardcoded bead IDs), `jleechan-7wud` (ZFC verdict enum).
  - Authored this activity entry; companion nextsteps doc at [`roadmap/nextsteps-2026-07-06-launchd-ci-fixes.md`](nextsteps-2026-07-06-launchd-ci-fixes.md) captures bead index, work queue, and merge state.
