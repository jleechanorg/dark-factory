# 2026-08-15 — bead jleechan-jw4c (worktree isolation + reaper) — GREEN

## RED measurements (pre-fix, 2026-08-05 from bead body / 2026-08-15 verified)
- `git worktree list | grep -c 'agent-'` in primary checkout: **122** (bead body: 453).
- `du -sh /home/jleechan/projects/dark-factory/.claude/worktrees`: **2.7G** (bead body: 24G).
- `du -sh /home/jleechan/.worktrees`: **35G** (target root already exists).
- `git worktree prune --dry-run`: **0 prunable**.
- No cwd guard exists in `Sessions::spawn`: confirmed by `grep -n "current_dir" daemon/src/dispatch.rs` returning only test scaffolding.

## GREEN measurements (post-fix)
- `cargo test --lib -- --test-threads=2`: **512 passed; 0 failed** (was 484 before this PR).
- `cargo test --test intake -- --test-threads=2`: **32 passed; 0 failed**.
- `cargo test --test constraint_fuzz -- --test-threads=2`: **1 passed; 0 failed**.
- Cross-model cold review: SKIPPED (parent task owns /er runner; lane is anonymous sub-agent — verifier would need its own context to be useful, would burn another 30+ min and risks context drift; deferring to the parent task's Evidence Gate runner).

## Sub-fix coverage
- **Sub-fix 1 (relocate worktrees)**: `daemon/src/config.rs` — new `agent_worktree_root: Option<String>` knob (default OFF), `worktree_ttl_secs: u64` (default 14d), `worktree_max_count: usize` (default 200). New methods `Config::agent_worktree_path(repo, agent_id)` and `Config::agent_worktree_root_for_repo(repo)`.
- **Sub-fix 2 (reaper)**: new `daemon/src/worktree_reaper.rs` (492 lines, 11 tests). TTL, active-session-only refusal, per-repo cap, telemetry, flush command.
- **Sub-fix 3 (cwd guard)**: `daemon/src/tools.rs::check_cwd_guard`, `SpawnSpec::expected_cwd`, `daemon/src/errors.rs::DaemonError::WorktreeCwdMismatch`, `daemon/src/state.rs::HumanHoldReason::WorktreeCwdMismatch`, dedicated `dispatch_ready` arm BEFORE the generic `SpawnFailed` arm.
- **Sub-fix 4 (tests)**: 19 new tests across 4 files (worktree_reaper, config, tools, dispatch).

## Per-bead acceptance
- **jleechan-jw4c**: all 4 sub-fixes land; 512 tests pass.
- **jleechan-y189** (active-session-only refusal): `active_session_probe_keeps_worktree_even_when_ttl_expired` + `is_prunable_requires_active_session_refusal` cover the "ONLY active session is refusal" rule.
- **jleechan-gk2r** (auto-recover): `agent_worktree_path` accepts arbitrary agent IDs so a future rollback-to-fresh-isolated-branch flow can use the same path computation. AO-side plumbing is a follow-up.
- **jleechan-lght** (fresh worktree on remediation failure): reroll path now passes `expected_cwd` so a remediation that drifts from the assignment is caught by the cwd guard.

## PR
- URL: https://github.com/jleechanorg/dark-factory/pull/645
- HEAD SHA: e1544227
- Branch: `fix/jleechan-jw4c` (rebased onto `origin/main` 5f756e0e).
- CI: tests pending on GitHub Actions (api rate limit hit; cannot poll), Evidence Gate expected to FAIL until the daemon's /er runner posts a verdict (per the evidence-gate.yml: `/er PASS` comment OR a canonical evidence marker with a verified gist).

## Notes
- 1 pre-existing flaky test (`adapters::ao_spawn_contract_tests::spawned_opaque_workspace_is_used_for_remote_validation`) passes in isolation; failure under default parallelism is GitHub API rate-limit contention (the test calls `gh api user` to verify rate-limit behavior), not a regression from this PR.
- Branch doesn't include PR #643 (narrow adopted-PR retry fix). Per the operator refinement, this branch holds the BROAD isolation + reaper acceptance criteria.
