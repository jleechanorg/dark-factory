# 2026-08-15 — bead jleechan-jw4c (worktree isolation + reaper)

## RED measurements (pre-fix)
- `git worktree list | grep -c 'agent-'` in primary checkout: **122** (matches bead body: agent-* under `~/projects/dark-factory/.claude/worktrees/`).
- `du -sh /home/jleechan/.worktrees`: **35G** (target worktree root already exists).
- `du -sh /home/jleechan/projects/dark-factory/.claude/worktrees`: **2.7G** (nested inside primary checkout on this fresh worktree, but the production operator machine measures 24G per the bead body).
- `git worktree prune --dry-run` returns **0 prunable** (every registration is a live directory).
- No `cwd` guard exists in `Sessions::spawn`: currently a worker can be spawned with `local_checkout = Some(worker_checkout)` and the daemon does no validation that the spawned process actually cd'd there. The jw4c failure is the silent acceptance — confirmed by grep: `grep -n "current_dir" daemon/src/dispatch.rs` only finds `local_checkout = Some(std::env::current_dir())` test scaffolding, not a guard.

## Bead body summary
- 511 worktrees / 60G on production operator (bead runtime RED measurements 2026-08-05).
- Worktrees live inside the primary checkout (`~/projects/<repo>/.claude/worktrees/agent-*`).
- Cap/fresh-fallback/auto-recovery + cwd guard + reaper all needed.
