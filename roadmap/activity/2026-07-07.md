# Activity — 2026-07-07

- **PR #188 opened for the durable daemon trigger slice**:
  - Branch `factory/jleechan-1m4-systemd-r1`, commit `565bc99`.
  - PR: https://github.com/jleechanorg/dark-factory/pull/188.
  - Scope: Linux `systemd --user` unit and installer, daemon `sd_notify`
    readiness/watchdog messages, explicit `ao_project` config for
    `worldarchitect`, and newly-intaken bead dispatch using the real tracker
    title instead of an empty prompt.

- **Safety corrections before publishing**:
  - Switched the unit from unconditional restart semantics to
    `Restart=on-failure`.
  - Set `WatchdogSec=7200s` so long LLM ticks and transient backoff sleeps do
    not turn into watchdog abort loops.
  - Added user-linger verification and PATH entries for `~/.npm-global/bin` and
    Node 22 tools.

- **Evidence status**:
  - Local verification passed: systemd installer shell checks, pytest installer
    checks, serial daemon cargo tests, clippy, graph audit, and diff check.
  - Full `/af` E2E remains unproven. PR #188 only addresses a blocker slice;
    it does not prove label/bead -> daemon intake -> AO worker PR -> gates ->
    READY/merge.
  - Last PR gate snapshot: Evidence Gate, CodeRabbit status, notify, and
    daemon-tests passed; `test` and `skeptic` were still in progress; Cursor
    Bugbot skipped due usage limit.

- **Tracking**:
  - `jleechan-1m4` moved to `in_progress` with PR #188 notes.
  - `jleechan-nzia` moved to `in_progress` with the explicit "no solid E2E
    proof yet" verdict.

