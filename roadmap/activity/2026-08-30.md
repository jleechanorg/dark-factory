# Activity — 2026-08-30

- Fixed and merged 3 real bugs in the reviewer-gate pipeline: PR #788 (`a5ad2b6fb6`, `gate_advice` missing `gate_strict="true"` in `ready.dot`), PR #789 (`79f8ecd606`, `parse_tool_free_codex_jsonl` rejecting codex-cli's benign `--dangerously-bypass-hook-trust` advisory, causing every default `/f` cold-review-v1 run to fail unconditionally), PR #791 (`778423e814`, `/code-standards`-flagged redundant double-validation between `_run_gate_once` and `_contract_adjusted_result` that masked PR #789's real error behind a misleading generic one).
- Deployed all 3 fixes to jeff-ubuntu's production `ai.dark-factory.daemon.service` (new immutable release, restarted, verified stable).
- Live-reproved the default `/f` pipeline's `cold_reviewer` now reaches `final_outcome: "success"` post-fix (was exhausting after 3 rounds pre-fix on the same transport bug).
- Surfaced and filed [rev-xfy23](https://github.com/jleechanorg/dark-factory) (see `br show rev-xfy23`): the default `/f` pipeline's `cold_reviewer` is tool-free by explicit design (its static prompt forbids all shell/browser/tool use); operator wants live tool access instead — unresolved architecture decision, not yet implemented. Full writeup: `~/roadmap/nextsteps-2026-08-30-factory-cold-reviewer-tool-access.md`.
