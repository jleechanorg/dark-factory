# Activity — 2026-08-31

- PR [#790](https://github.com/jleechanorg/dark-factory/pull/790) advanced to `e4c6513727616210a639301171403f3dae60e4e0`: project ownership is persisted with live AO sessions and explicit project-scoped cleanup/status paths gained local serialized test coverage.
- Addressed independent review followups:
  1. Replaced raw `ao status` probe in `tick.rs` with project-scoped `Sessions::session_pr_number_in_project`.
  2. Made `overlay_session_project` fail closed on legacy rows lacking `session_ao_project` instead of falling back to mutable routing.
  3. Paired `session_ao_project = None` with every `session_id = None` clear in memory across `reroll.rs` and `dispatch.rs`.
  4. Fixed CI clippy errors in `reroll.rs` and `state.rs`.
  5. Added SQLite restart regression test in `tick_integration_sqlite.rs` verifying legacy unowned sessions fail closed without foreign-project kills.
- `/nextsteps` tracker update is blocked safely: `br where` redirects this checkout to the WorldArchitect Beads database, so no Dark Factory bead was created or changed.

