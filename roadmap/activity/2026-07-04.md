- **Continuation box (2026-07-04)**: Waves 1-2 merged (9/10 Stage-1 tasks, 4-way parallel coder wave proven ~25min); gate-6 /er rubric; tick bg-wait fix; ultracode hardening sweep launched. Lessons: label clobber starves queue; rebase+push+verify against factory-moving main; stubs manufacture parallelism.
- Hardening: ultracode sweep 22 confirmed; 8 harness fixes verified + 7 beaded (2 factory-fixed already: #152/#153); Stage-1 daemon code-complete (#139-#151); bead-closed-check merge-vs-abandon race fixed.
- **Parallelization diagnosis**: measured telemetry — Wave-2 4 coders ran concurrently 4-7min each (parallel proven); slowness is fixed tick sleeps (~25min waiting/bead for ~5min work) + box-restart idle, NOT compute. Fixed: coder tick 600→240s, verifier 300→120s. Stage-1 daemon code-complete + factory self-repairing its own crate bugs (#152/#153 merged, #154 open).
- **/advice + /harness ops pass**: strategy (a)+guards-first (no unattended Rust cutover). Landed safe-push-main.sh + auto-merge-guard.sh (no-red policy caught PR#150 red-gate merge). Beads ef60 (cross-wave Rust conflicts), p944 (guards need cron callers).
- **Cutover decision (/history+/harness+/advice)**: factory-lite retired from active duty — loops dead at max_wall (575min silent gaps), all wave PRs merged, daemon blocked only on 5 production adapters (main.rs NoopAdapters). Verdict (b): direct-code adapters (jleechan-732a), supervised --once shakedown, decommission by 2026-07-11 (jleechan-xrdx), SLO monitor (jleechan-k3zh). Handoff: ~/roadmap/nextsteps-2026-07-03-auto-factory-bootstrap.md §Cutover.
