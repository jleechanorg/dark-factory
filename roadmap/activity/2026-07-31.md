# Activity — 2026-07-31

## Dark-factory controller-owned cold review — paused mid-slice on `feat/controller-owned-cold-review`

- **Repo:** [jleechanorg/dark-factory](https://github.com/jleechanorg/dark-factory) (worktree `/Users/jleechan/projects/worktree_factory_cold`)
- **Branch:** `feat/controller-owned-cold-review` (no PR open yet — `gh pr view` returned `no pull requests found for branch "feat/controller-owned-cold-review"`)
- **Driver:** Codex Spark (`gpt-5.3-codex-spark`) via `.dark-factory/codex-spark-bin/codex` shim, invoked by `bin/dark-factory` against a saved slice pipeline under `.dark-factory/`
- **Origin:** [Slack C09GRLXF9GR/p1785487304101909](https://jleechanai.slack.com/archives/C09GRLXF9GR/p1785487304101909) — reviewer proposal patch (`F0BMYMNFK7A` + `F0BM82P0XGU`) was supplied as input only; root-cause review revealed it bypasses receipt enforcement and in-graph controller lanes, so the patch was applied as a starting point and corrected via dependency-safe vertical slices.
- **Three-slice plan:** (1) wrapper, (2) controller core, (3) safe controller transport; (4–6) read-only target, graph integration, docs remain.

### Slice outcomes (current run, in chronological factory-log order)

1. **`slice_wrapper.dot` — DONE, committed + pushed** — `e2f97f56c` ([commit](https://github.com/jleechanorg/dark-factory/commit/e2f97f56ca8b4ec9eaef333cbf36143c2e6e701a)) `feat(review): route read-only review subcommand safely`. Touches `bin/dark-factory` (113 lines) + `tests/test_slash_command_binary_contract.py` (161 lines). Authoritative checks green: `bash -n bin/dark-factory`, `./bin/dark-factory review --help`, 11 contract tests. Run `b4307fed507c` final=success.
2. **`slice_controller_core.dot` — DONE, committed + pushed** — `e7e2cea50` ([commit](https://github.com/jleechanorg/dark-factory/commit/e7e2cea5073f11ccfb8a3be9660f120ea51b984a)) `feat(review): harden controller-owned contract core`. Touches `runner/review_controller.py` + `prompts/catalog/controller_cold_review_v1.md` + `tests/test_review_controller.py`. 36 unit tests + sealed hello holdout 2/2 + AGY review PASS at exact HEAD. Run `7196f2769101` final=success.
3. **`slice_controller_transport.dot` — DONE in factory log, NOT committed/pushed** — Run `b9551285b266` final=success steps=14 duration_ms=965566 (≈16 min), 2 review→fix cycles that recovered (seq 5, seq 9 fixes restored PASS at seq 12). Diff in worktree but never committed because the Codex CLI weekly quota was exhausted during the wrapper session before the worker could finalize the commit/push. Files: `runner/handler_dispatch.py` (+35 unstaged on top of Slack patch's +174 staged), `runner/handlers.py` (+1), `tests/test_gate_subprocess_dispatch.py` (+243 unstaged on top of Slack patch's +44 staged). `handler_sandbox.py` deliberately NOT touched.
4. **Remaining slices (4–6):** read-only target / immutable workspace validation, graph integration (CLI + graph share one controller request/execution/artifact implementation; no duplicate snapshot/receipt logic), docs.

### Blocker / risk recorded

- `br list` is currently broken with `Duplicate external_ref: jleechanorg/worldarchitect.ai#8227`. Beads updates were skipped this run; queue state lives only in the nextsteps doc.
- A Codex GUI session (PID 52954, kernel `c73c4e43ec494f818b4f27ac5e88ec56`) is still alive; harmless but consumes the weekly quota. Closed during pause.
- The `dark-factory/daemon/factory-af-tick.sh` (PID 39832) was unrelated auto-factory activity from a different repo; left running.

### PR / merge state

- No PR open on `feat/controller-owned-cold-review` — must be opened after transport slice is committed + slice 4 (immutable target) lands. PR-body template per `.claude/skills/pr-description-sections/SKILL.md`.
- [PR #407](https://github.com/jleechanorg/dark-factory/pull/407) — MERGED 2026-07-21 (`feat(reviewer): opt-in reproduction-receipt gate for parallel reviewer verdicts`, different feature). All CI checks green at merge.

### Next session entry point

Resume in `/Users/jleechan/projects/worktree_factory_cold`:

1. `git status -sb` (expect branch `feat/controller-owned-cold-review`, ahead of `origin/main` by 2 commits, with the transport slice diff unstaged/staged).
2. Verify `tests/test_gate_subprocess_dispatch.py` is fully green in isolation (`./.venv/bin/python -m pytest -p no:cacheprovider tests/test_gate_subprocess_dispatch.py`).
3. `git add` the transport slice files and commit with attribution `[codex/gpt-5.3-codex-spark]`.
4. Open the next slice (immutable target): `.dark-factory/slice-immutable-target.md` (NOT YET WRITTEN) + a dedicated `.dark-factory/slice_immutable_target.dot`.
5. After slice 4 is green, run `gh pr create` to open the PR; drive `/green` from there.

Full handoff in `~/roadmap/nextsteps-2026-07-31-controller-owned-cold-review.md` (this session's independent doc).