# Activity — 2026-07-31

## Dark-factory controller-owned cold review — completed and PR #490 OPEN

- **Repo:** [jleechanorg/dark-factory](https://github.com/jleechanorg/dark-factory) (worktree `/Users/jleechan/projects/worktree_factory_cold`)
- **Branch:** `feat/controller-owned-cold-review` (head `a11004fc0`)
- **Driver:** Claude (MiniMax-M3 via Codex CLI), ultracode effort level. Codex Spark coder workers hit weekly quota mid-transport-slice; implementation completed directly by Claude after the factory circuit-breaker tripped.

### Slices (cumulative commits)

1. Wrapper — `e2f97f56c` ([commit](https://github.com/jleechanorg/dark-factory/commit/e2f97f56c)) — `bin/dark-factory`, `tests/test_slash_command_binary_contract.py`
2. Controller core — `e7e2cea50` ([commit](https://github.com/jleechanorg/dark-factory/commit/e7e2cea50)) — `runner/review_controller.py`, `prompts/catalog/controller_cold_review_v1.md`, `tests/test_review_controller.py`
3. Safe transport — `2742d99a9` ([commit](https://github.com/jleechanorg/dark-factory/commit/2742d99a9)) — `runner/handler_dispatch.py`, `runner/handlers.py`, `tests/test_gate_subprocess_dispatch.py`
4. Immutable target — `f21befaee` ([commit](https://github.com/jleechanorg/dark-factory/commit/f21befaee)) — `runner/review_controller.py:validate_immutable_target`, `tests/test_immutable_target.py` (14 tests)
5. Graph integration — `96d7fbd01` ([commit](https://github.com/jleechanorg/dark-factory/commit/96d7fbd01)) — `runner/review_controller.py:run_controller_review`, `runner/handler_parallel_reviewer.py:lane_output_dir`, `tests/test_graph_controller_integration.py` (6 tests)
6. Docs — `d51f9e369` ([commit](https://github.com/jleechanorg/dark-factory/commit/d51f9e369)) — `.claude/skills/reviewer-calibration/SKILL.md`, `.claude/workflows/dark-factory.md`
7. **Post-review fixes (Opus findings)** — `8a22ea61b` ([commit](https://github.com/jleechanorg/dark-factory/commit/8a22ea61b)) — wires `validate_immutable_target` into `_controller_review_request` AND `review_cli.py:main` (was defined-but-unused)
8. PR body — `d132a112f` ([commit](https://github.com/jleechanorg/dark-factory/commit/d132a112f))
9. Evidence bundle — `20c5c4611` ([commit](https://github.com/jleechanorg/dark-factory/commit/20c5c4611))
10. Evidence marker — `d0414930f` ([commit](https://github.com/jleechanorg/dark-factory/commit/d0414930f))
11. Activity log — `a11004fc0` (this commit)

### PR / merge state

- [PR #490](https://github.com/jleechanorg/dark-factory/pull/490) OPEN at head `a11004fc0`, MERGEABLE.
- CI: Evidence Gate SUCCESS, daemon-tests SUCCESS, CodeRabbit SUCCESS, test pending (re-trigger after the wiring fix).
- `/advice` A1 (Opus subagent, codex CLI weekly quota exhausted) verdict on `d132a112f` (pre-wiring-fix head): **"Ship for the three opt-in pipelines"**, confidence **high**.
- Two real defects found by Opus after the initial 6-slice PR was opened:
  - `validate_immutable_target` was defined but not called from production paths (both `_controller_review_request` and `review_cli.py:main`). **Fixed in `8a22ea61b`.**
  - `DARK_FACTORY_FAKE_LLM` and `DARK_FACTORY_ITERATION_STUB` env vars are unread by the controller, leaving a structural blind spot for stub-mode READY transitions. **Deferred — this is the only gap PR #490 does not close.**

### Per-instance /advice verdict table

| # | Instance | Fixed by PR #490? | Residual gap |
|---|----------|-------------------|--------------|
| 1 | PR #26 priority-queue dispatch decoration | Yes (post-`8a22ea61b`: codex subprocess required, JSONL receipts typed, transport rejects `--yolo`/unsafe sandbox, response verdict mechanically reconciled, holdout-root rejection wired in production) | The priority queue itself (`_gate_subprocess_args`) still uses `--yolo` for non-cold-review lanes |
| 2 | PR #93/92 G10 patterns (post-closeout drift) | Partially — `C2`/`C5`/`E2`/`E3`/`E10` target the class | Model owns interpretation; no "required-artifacts" manifest |
| 3 | PR #134 missing cutover doc | Mostly — `C1`/`C2`/`E9`/`E13` catch missing docs | Model must read the spec; no "required-docs" enforcement |
| 4 | Stub vs real-mode READY distinction | **No** — neither `DARK_FACTORY_FAKE_LLM` nor `DARK_FACTORY_ITERATION_STUB` is referenced in `runner/` | Structural blind spot; deferred to a follow-up PR |
| 5 | Non-converging fix loops | Indirectly — `C0`/`C4` + `_verify_controller_workspace` make silent WIP stacking harder | Engine `max_visits="3"` is the hard stop; controller does not raise it |

### `/history` deep search — last 30 days, top 5 /f underperformances

| # | Date | Repo / PR | Gap |
|---|------|-----------|-----|
| 1 | 2026-07-31 | worktree_factory_cold (current PR #490) | /f in-pipeline reviewer deemed untrustworthy as sole authority → real Codex reviewer added |
| 2 | 2026-07-31 | worktree_factory_cold PR #8489 (CR7) | Self-verification was author-leaning; replaced with real cross-model cold review |
| 3 | 2026-07-31 | worktree_factory_cold | Spark coder mega-patch unsafe; vertical slicing required |
| 4 | 2026-07-31 | worktree_factory_cold | `bin/dark-factory review` sliced separately from contract core (3 Codex threads, not one /f mega-PR) |
| 5 | 2026-07-30 | tybolt-roadmap-docs (worldarchitect.ai PRs) | Standing adversarial Codex reviewer used because /f alone misses "defect class a Claude reviewer would systematically miss" |

Sources: 5105 Claude Code JSONLs, 26603 Codex threads, Hermes FTS5 (89 matches), Antigravity (none in window), OpenCode (0), Cursor (5 — all from 2026-06-23, outside window).

### Sidekick state

- `/Users/jleechan/roadmap/dark-factory/sidekick/feat-controller-owned-cold-review/STATE.md` and `sidekick.prompt.md` are durable on disk for crash recovery. The 3-hour autonomy time-box did not apply because the user explicitly asked for `continue` after each pause.

### Blocker recorded

- `br list` is currently broken with `Duplicate external_ref: jleechanorg/worldarchitect.ai#8227` (cross-repo leak from a prior auto-factory canary batch). Beads updates were skipped. Recovery is operator-grade (preserve `.beads/.br_recovery/` evidence, dedup the offending rows in `.beads/issues.jsonl`, rebuild).

### Resume in next session

PR #490 is MERGEABLE. Operator may merge when ready. Head `a11004fc0`. If a follow-up PR is opened for instance 4 (stub-mode env vars), open it on `feat/controller-owned-cold-review` or a fresh branch from `main` after merge.

Nextsteps doc: `/Users/jleechan/roadmap/nextsteps-2026-07-31-controller-owned-cold-review.md`
Evidence gist: <https://gist.github.com/jleechan2015/fb3a17b7e2ea7abda1d8bbec237d2b3f>
Claude auto-memory: `/Users/jleechan/.claude/projects/-Users-jleechan-projects-dark-factory/memory/project_2026-07-31_controller_owned_cold_review.md`
Learnings: `/Users/jleechan/roadmap/learnings-2026-07.md` (entry `2026-07-31 — Controller-owned cold review vertical slicing`)