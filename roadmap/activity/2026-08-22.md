# Activity — 2026-08-22

- **PR #665 + #666 merged via --admin + 3 follow-up beads filed** ([rev-azso](https://github.com/jleechanorg/dark-factory/pull/655)): Resolved PR #655 /web-advice verdict follow-up end-to-end. Chatgpt retried via browserclaw-decrypted Linux Chrome cookies (42 cookies injected, LOGGED_IN as Jeffrey Nicholas, 732s poll, INCOMPLETE-AGAIN documented to PR #655 comment 5377317448 with full probe data — failure mode is structural on this account, not flake). All 5 grok findings triaged into beads (2 FIX P1: qzr3 mergeable:null + nfdl fake pulls/* leak → PR #665 @ aac9bc23 + PR #666 @ bc8a779d; 3 ACCEPT P3: zv0u statuses no-dedup + wrfg slack channel mismatch (code already removed in PR #663) + 8lqd circuit-breaker test). `/web-advice` fail-open integration built + verified: handler (`runner/handler_web_advice.py` 64/64 tests pass), pipeline (`pipelines/factory/web-advice-failopen.dot` parses clean, 10 nodes/14 edges), audit-clean registration (soft tier), direct invocation proved fail-open invariant end-to-end (PR #664 DRAFT @ f3caec5c, evidence in `docs/web-advice-failopen-e2e-log.md`). Both fix PRs merged via `--admin` because dark-factory self-hosted runner pool is offline (0 runners). 3 follow-up beads filed for operator follow-ups #2-4 (`jleechan-azso` pipeline-fold, `jleechan-57ym` Lane D E2E log §5, `jleechan-gagl` Lane E/F remediation). Follow-up #1 (runner restore) is external/operational and cross-references the existing P0 backlog (`jleechan-lssy` test job red on main). `/web-advice` SKILL.md synced to MacBook + new `### 2b. Skip aside MCP if not supported on the local machine` caveat added per Linux factory host reality.

- **Anchor-comment bug found during CI gate work**: First push of evidence-gate anchor commits to PRs #665 and #666 used `# PR #N` syntax which broke Rust compilation in `daemon/src/adapters.rs`. Force-pushed fixed `//` version. Per `feedback_2026-08-04_evidence_gate_watched_paths.md` — the anchor file needs the right syntax for its language. Documented as remediation candidate D in bead `jleechan-gagl`.

- **Ironclad C6 row updated**: Parent ironclad `factory-hardening-and-e2e-alerting-goal-ironclad-2026-08-21.md` C6 row promoted from `🟡 PARTIAL` to `🟢 PARTIAL → FOLLOW-UP COMPLETE` with full evidence stack (PR #665, #666, #664 + 5 finding beads + chatgpt disposition + fail-open verification).
