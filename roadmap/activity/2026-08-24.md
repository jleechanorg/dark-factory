# Activity — 2026-08-24

## Verified handoff correction

- **Runner scope corrected:** `gh api /repos/jleechanorg/dark-factory/actions/runners` returns zero because the self-hosted fleet is organization-scoped. The latest organization probe returned 14 online runners (1 busy); queued and in-progress repository workflow lists were empty. No runner-restoration blocker remains.
- **Tracking corrected:** Issue [#286](https://github.com/jleechanorg/dark-factory/issues/286) is CLOSED and selector fix [PR #287](https://github.com/jleechanorg/dark-factory/pull/287) is MERGED. Bead `jleechan-xn4n` remains OPEN for an unrelated Linux-container `sqlite3` portability defect associated with PR #577; it is not runner-restoration tracking. Bead IDs are kept as plain identifiers rather than invalid GitHub issue URLs.
- **Canonical web-advice candidate:** [PR #742](https://github.com/jleechanorg/dark-factory/pull/742) is the current-main web-advice/Lane-D superset. It includes the canonical `--require-holdouts` invocation test and a root-cause repair that validates the CDP `/json/version` endpoint independently of Aside CLI. #691/#693/#700/#701/#707/#708/#711/#715/#718/#723 are duplicate or subset generations; none should be merged in parallel.
- **Rate-limit remediation landed:** [PR #734](https://github.com/jleechanorg/dark-factory/pull/734) fixed generic-403 classification, passed exact-head CI/evidence/independent review and real-browser Gemini+Grok approval, and merged as `96f8d82055b9530324c861ef57620aedbf99847f`. Duplicate generations #692/#698/#702/#706/#710/#713/#717/#720/#727 were closed with branches retained.
- **Independent remediation:** [PR #694](https://github.com/jleechanorg/dark-factory/pull/694) has the requested environment-isolation, whitespace, and manual-hook wording cleanup at `6988c4731fb306c1f8c37f19e4192c6c712e2d9f`; exact-head review and CI remain before landing.
- **Execution order:** finish #742's design implementation and real-browser panel; then forward-merge/land #694 after #742 because they share an E2E document; finally close the remaining duplicate PRs. Issue #286 is already reconciled and closed. Handoff priority is operational sequencing and is distinct from Bead priority metadata.

## Earlier roadmap work

- [PR #714](https://github.com/jleechanorg/dark-factory/pull/714) merged the prior roadmap synchronization.
- [PR #740](https://github.com/jleechanorg/dark-factory/pull/740) merged the first version of this day’s handoff; this file records the subsequent scope and PR-state correction.

## Three-lane funnel analysis + repeatable CLI (later, same day)

- **Ran `/factory-funnel` for 24h, then 3d, then a corrected 30d 3-lane split.** The first 0/359 result was invalid because the origin join was attempt-scoped. The corrected bead-level result is **2 of 409 (0.5%)**: `bead_start`=1/142, `gh_issue_start`=0/156, `pr_adopted_start`=1/111.
- **Per-gate breakdown**: 0 of 67 GATE_ASSESSMENT events (3d) had `all_green=true`. Worst offender previously untracked: `coderabbit` stuck at 94% unknown ("CodeRabbit review is still pending/unknown" never resolves) — filed bead `jleechan-evtv`. Other chronic gates already tracked: `skeptic` 70% fail (`jleechan-5arc`), `vacuous_red_green` 80% unknown (`jleechan-6xje`/`jleechan-sk55`, PR #570 in flight).
- **pr_adopted_start is the best-performing intake lane** (1/111 reaches READY_FOR_MERGE and existing PRs reach assessment early), but the overall rate remains below 1%; gate assessment remains the dominant measured bottleneck.
- **Built repeatable CLI**: `runner/funnel_lanes.py` + `bin/df-funnel-lanes`; exact-head verification collected 35 focused tests (34 passed, 1 skipped) after the cross-attempt regression. Opened [PR #744](https://github.com/jleechanorg/dark-factory/pull/744), which is not ready while Evidence Gate is red, external review is quota-limited, and a current review thread shows the new binary is absent from `install.sh`'s install lists.
- **Updated `.claude/skills/factory-funnel/SKILL.md`** with the 3-lane methodology (§4) and a mandatory "always run 24h AND 3d, never just one" rule to prevent future misleading single-window reports.

## Correction — funnel_lanes bug found + fixed (same session)

- **Self-caught bug**: `runner/funnel_lanes.py`'s `classify_origin` joined origin events to downstream stage events by `(bead_id, attempt_id)`, silently dropping any bead whose origin event fired on an early attempt but whose `READY_FOR_MERGE` fired on a later reroll attempt. The "0 READY_FOR_MERGE across all 3 lanes in 30d" claim was **wrong** — corrected number: **2 of 409 beads (0.5%)** reached READY_FOR_MERGE (`bead_start`=1, `pr_adopted_start`=1, `gh_issue_start`=0). Fixed in [PR #744](https://github.com/jleechanorg/dark-factory/pull/744) with a regression test reproducing both real-world instances (`dark-factory-4sey`, `jleechan-l3r6`). Fresh verification reports 34 passed and 1 skipped (35 collected).
- Also separately corrected an earlier manual (non-scripted) claim that "last READY_FOR_MERGE fired 2026-07-24, 31 days stale" — that was a bug in an ad-hoc Python one-liner that only checked the `timestamp` field, missing legacy-schema rows using `ts`. The real last event (properly schema-tolerant) was 2026-08-19, only 5 days before the check.

## Existing-PR dispatch failure and parked direct-work handoff

- Filed [issue #743](https://github.com/jleechanorg/dark-factory/issues/743): AO force-checkout fails when a drive-existing-PR branch is already owned by another valid worktree, after the overlay has already recorded a false `DISPATCHED` state.
- Removed `factory` labels and parked four affected beads: `dark-factory-cfla` (#8931), `dark-factory-a98p` (#8934), `dark-factory-vavc` (#8935), and `dark-factory-3al0` (#9300). They must not be re-routed until #743's ownership/adoption regression and live canary pass.
- Preserved the direct-work dependency: #8935's canonical `world_time` correction precedes #8931's rest-choice/provenance evidence; #8934 `/spells` canonicalization and #9300 authoritative XP application are independent.

## Funnel metrics refresh and remediation drafts

- **Live PR correction:** #742 and #744 are now **MERGED** (#744 at `422e86bc5e2c04df3af23c27ebbece5b2d000c31`); the earlier OPEN/NOT READY wording is historical only. #748, #749, and #750 are the active OPEN draft funnel candidates; #746 remains a preserved draft with a failing verified-state race test and is deprioritized.
- **Corrected current baseline:** fresh 30d telemetry is 2/412 `READY_FOR_MERGE` (0.485%): `bead_start` 1/142, `gh_issue_start` 0/156, `pr_adopted_start` 1/114 (0.877%). Fresh 3d CodeRabbit exact-head accounting is 1 direct approval, 3 vendor-unavailable waivers, and 64 unknown among 68 unique PR-heads (1.47% direct; ironclad criterion 1 FAIL); fresh 30d is 37 direct, 12 waived, 291 unknown, 16 fail among 356.
- **Next order:** independently review #748 reporting, #749 exact-head binding, and #750 gate-8 pytest routing; keep direct approvals separate from waivers/unknowns; re-run 24h+3d operational and 30d lane windows after candidates land; do not claim any ironclad criterion or 48h sustain until external GitHub evidence verifies it. Beads `jleechan-4n2e`, `jleechan-evtv`, `jleechan-6xje`, and `jleechan-sk55` were annotated; no duplicate bead/issue was created.

## Verified funnel readiness refresh (2026-08-25)

- **Live metrics:** fresh 3d `READY_FOR_MERGE=0`; CodeRabbit direct substantive observations are 1/58 (1.72%) with quota/rate-limit/vendor-unavailable outcomes kept distinct. Fresh 30d `READY_FOR_MERGE=2/412` (0.485%); `pr_adopted_start=1/114` (0.877%). The 48-hour sustain check has not started.
- **PR state:** #748 is OPEN/non-draft/clean at `155d344ed1de5c64c6df8c9baef34e4b910810b4`; #749 is OPEN/non-draft/clean at `4035ba0dbd4274f43846420abc400f62c2e91377`; #750 is OPEN/non-draft/clean at `c0cadf76f4edde2c2c0ce234ffcba301fa2889d4`. Evidence Gate and required checks are green on each exact head; none claims a substantive CodeRabbit review. #743 remains OPEN and factory routing is removed from its four parked beads.
- **Structural blocker:** CodeRabbit is operational, but this account is quota/rate-limited. Treat that external condition as criterion-7 blocker; do not reinterpret quota responses as daemon defects or weaken gates.
- **Next internal work:** `comments_resolved` failed 56/71 fresh 3d assessments (78.9%). New non-factory P1 bead `jleechan-cqaf` covers enriching unresolved thread bodies/paths/IDs as untrusted feedback, preserving Unknown on GraphQL errors, and filtering/prompt tests. Skeptic caller telemetry is a separate startup defect: 149/149 fresh 3d/7d runs are `startup_failure` with `jobs=[]`, all six pin variables absent. New non-factory P1 bead `jleechan-uqgu` covers pre-job diagnosis, config-check CI materialization, and safe absent-pin skip semantics; do not call this a skeptic policy failure.

## Final funnel/PR readiness refresh (2026-08-25)

- **Fresh funnel metrics:** 3d `READY_FOR_MERGE=0/38`; CodeRabbit `1/61` direct, `4` waived-unavailable, `56` unknown. 7d `READY_FOR_MERGE=1/104` (0.962%), `pr_adopted_start=1/18` (5.56%), CodeRabbit `4/147` (2.72%), and `all_green=true` fired 3 times. 30d `READY_FOR_MERGE=2/413` (0.484%), `bead_start=1/142`, `gh_issue_start=0/157`, `pr_adopted_start=1/114` (0.877%), CodeRabbit `37/359` (10.31%) with 13 waived, 294 unknown, and 16 fail. No funnel improvement is claimed.
- **Runner correction:** organization-scoped self-hosted fleet is healthy at 16/16 online with 0 queued runs; the repository-scope empty result remains a false alarm, not a restoration task.
- **Ready PR set:** #748 `155d344e`, #749 `673ab110`, #750 `84bb15ab`, #751 `c8110484`, and #752 `c4eb4793` are each OPEN/non-draft/CLEAN with green applicable checks, exact-head evidence, Gemini/Grok approvals, and zero unresolved threads. #750 includes the fixture/helper materialization correction; #752 includes full untrusted-feedback transport, bounded read-only preflight, fail-closed pagination, and TOML-safe reroll persistence. None is merged or deployed; CodeRabbit is rate-limited/non-substantive on all five.
- **Ironclad status:** C1 FAIL (1/61 direct in 3d), C2 telemetry PASS but caveated (3 `all_green` in 7d, pending post-landing verification), C3 FAIL (0.484%), C4 FAIL (0.877%), C5 evidence clean but not landed, C6 not started, C7 blocked by CodeRabbit quota/rate limit.
- **Bead mapping:** `jleechan-uqgu` → #751; `jleechan-cqaf` → #752. Keep `jleechan-4n2e` and `jleechan-evtv` open. Keep #743 OPEN with factory routing removed and the four beads parked/unlabelled.
- **Next action:** merge/deploy authority is still absent. After authorized landing, recompute all funnel windows and begin the independent 48-hour sustain check; do not claim funnel improvement or goal completion before it passes.
