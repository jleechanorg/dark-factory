# Activity — 2026-07-10

- **PR #7888 driven from fully red to `mergeable_state: clean`** (dark-factory
  `/af` end-to-end proof mission, continuing from 2026-07-08's provisional C2
  claim, now downgraded to INSUFFICIENT):
  - Discovered and fixed 6 sequential daemon/AO infrastructure bugs that had made
    stage-2 adopted-PR remediation structurally non-functional for any real target
    repo: gate-7's dual-reviewer dispatch never fell back to a healthy 3rd vendor
    when codex+claude+agy all quota-exhausted simultaneously (fixed via gemini as
    a 4th vendor, [dark-factory PR #216](https://github.com/jleechanorg/dark-factory/pull/216));
    `CliVcs::remote_head_sha`/`is_ancestor` ran git against the daemon's own repo
    instead of the target repo ([PR #217](https://github.com/jleechanorg/dark-factory/pull/217));
    a circuit-breaker-parked bead's recovery got unconditionally overridden 21s
    later ([PR #212](https://github.com/jleechanorg/dark-factory/pull/212));
    `ci_pending` silently defaulted true on GraphQL+REST fetch degradation, causing
    an infinite `VERIFICATION_PENDING` loop ([PR #213](https://github.com/jleechanorg/dark-factory/pull/213));
    a HUMAN_HELD bead with no valid SCM comment target silently lost its
    escalation forever ([PR #215](https://github.com/jleechanorg/dark-factory/pull/215));
    a zombie AO session stuck "spawning" for a full day was falsely treated as
    active; AO's own orchestrator process had no persistent service and died
    repeatedly whenever its launching tool call's lifecycle ended (fixed with a
    proper `systemd --user` unit, `Restart=always`, live-tested via `kill -9`).
  - Once the infra was healthy, the real `/af` coder loop (Claude Fable model,
    session `wa-3084`) produced 5 genuine, independently-verified fixes on PR#7888:
    `apt-get update` before Playwright's dependency install (stale apt index on
    self-hosted runners), a full apt-state-directory redirect for a
    different-permission runner, raising a coverage-report job's timeout 5→15min,
    installing Node.js for the `mcp` test matrix group (the real root cause of what
    initially looked like a load-flake — it was a deterministic missing setup step),
    and a resync merge with a fast-moving `main`.
  - Found and manually unblocked a structural gap: Gate 8 (`Smoke Gate Wait`)
    requires an explicit real-mode `/smoke` test that nothing in `/af`'s
    remediation loop ever triggers — every PR reaching this gate hits a permanent
    25-minute-timeout wall without manual intervention
    (`gh workflow run mcp-smoke-tests.yml -f pr_number=7888 -f test_mode=real`).
    Filed as [jleechan-ltdc](https://github.com/jleechanorg/dark-factory) (P0) for
    a real daemon-side fix.
  - Portability audit (operator directive: `/green`/`/er` must be self-contained,
    no `~` dependencies, machine-portable): found `/green`/`/er` are not defined
    inside either repo at all (only in `~/.claude/commands/`, jleechan-tjio, P1);
    `DARK_FACTORY_HOLDOUTS`'s sibling-repo-path default silently produces an
    empty sandbox deny-list on a machine without it set — a real security-relevant
    silent failure for the sealed-holdout guarantee (jleechan-9k3a, P0); a
    hardcoded nvm Node version in 4 places (jleechan-pvgd, P2, low urgency).
  - Also filed jleechan-3bpe (AO worktree can attach the wrong repo's git remote),
    jleechan-r28r (23 duplicate/malformed-external_ref beads causing 390 failed
    escalation attempts/15min — 10 fixed mechanically, ~10 revealed as genuine
    duplicate beads needing a consolidation pass), jleechan-twa0 (escalation
    parser only accepts one external_ref format), jleechan-wxti (Green Gate
    Precheck fails closed on its own ~5.5min poll timeout rather than
    distinguishing "genuinely failed" from "still pending").
  - Corrected and closed jleechan-91vv: initially filed as a load-related test
    flake, corrected once the real fix (Node.js install) landed and proved it was
    a deterministic bug, not intermittent.
  - Session co-driven with a persistent `sidekick` orchestrator teammate across
    ~45+ waves, with extensive independent cross-verification in both directions
    (not trusting either side's self-reports) — caught and corrected several false
    positives along the way (misread CI cancellation-vs-timeout signals, a
    parsing bug in one own's monitoring script, cross-run job-rerun result
    mismatches).
  - PR#7888 not yet merged — `mergeable_state: clean` achieved but merge itself is
    correctly human-approval-gated per this repo's standing rule for product
    repos, not auto-merged.
  - 5 additional top-priority PRs (jleechan-rzgl/PR#7999, jleechan-ywab/PR#8177,
    jleechan-msqd/PR#8289, jleechan-7re5/PR#8189, jleechan-b8wj/PR#8036) queued
    and actively dispatched via `/af` in parallel; none reached green yet as of
    this entry.
  - Full detail: [Nextsteps 2026-07-10](../../../roadmap/nextsteps-2026-07-10-af-e2e-mission.md)
    (home roadmap), ironclad exit criteria at
    `goals/2026-07-07-1915-af-e2e-proof/02-ironclad-exit-criteria-pr7888.md`.

- **Independent 48-hour `/af` audit correction**:
  - Verdict: current Pipeline/PR-lifecycle E2E proof is **INSUFFICIENT**. The
    evidence lacks the required recording and complete lifecycle/cleanup chain;
    the only READY telemetry has `all_green=false` with no later
    `all_green=true` attestation.
  - Runtime health: 63,069 failed escalation notifications, 41,398 repeated
    adoption events, 670 dispatch errors, 54 current `HUMAN_HELD` rows, and an
    AO queue that reached 100 pending requests in the audit window.
  - New blockers: `jleechan-haux` (P0 Linux isolation), `jleechan-7t92` (P0
    structured-state render crash), `jleechan-kk64` (P0 unresolved-thread gate
    fails open), and `jleechan-l4ki` (P1 `/af` readiness-contract drift).
  - Existing escalation defects `jleechan-r28r` and `jleechan-twa0` were raised
    to P0. `jleechan-sniw.2` stays open until a self-created, recorded canary
    proves intake through terminal outcome and cleanup.
