Review the implementation as a skeptical senior engineer.

Goal:
${goal}

Use the current repository state and `spec.md`.

Return a concise verdict:
- `success` only if the feature is ready for holdout validation.
- `failure` if correctness, security, maintainability, or evidence gaps remain.

List concrete findings with file paths and exact remediation.
