Implement the feature described in `spec.md`.

Goal:
${goal}

Rules:
- Prefer test-driven changes where practical.
- Preserve existing behavior unless `spec.md` explicitly requires a change.
- Keep edits focused.
- Record changed files and evidence in your final response.
