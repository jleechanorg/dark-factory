"""Dark Factory — DOT-based pipeline runner (Attractor pattern).

Parses Graphviz DOT files into pipeline graphs and executes them
with pluggable node handlers, checkpointing, and human gates.
"""

__version__ = "0.1.0"
